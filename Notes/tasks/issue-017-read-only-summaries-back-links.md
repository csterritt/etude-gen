# Tasks for #17: Read-only prior-answer summaries and Back links on later steps

Parent issue: #17
Parent PRD: `Notes/PRD-etude-generator.md`

## Tasks

### 1. Write failing Bun tests for `deriveStepSummary`

**Type**: RED
**Output**: New `tests/etude-summary.spec.ts` that fails because `deriveStepSummary` does not exist yet. The tests assert the pure summary model for each step level, that invalidated/stale values are filtered out, that null/empty selections yield empty arrays (never a stale display), and that the function is pure.
**Depends on**: none

Before writing any production code, read and follow the coding standards in `Notes/skills/AGENTS.md` and the project `AGENTS.md` (red-green-refactor, `bun:test` with `describe`/`it`/`expect`, arrow functions, explicit types, no `any`, kebab-case filenames, braces around all `if`/`while` bodies). Read `Notes/skills/code-writing/typescript-rules` for the TypeScript conventions. Look at `tests/music-domain.spec.ts` and `tests/split-boundary-validator.spec.ts` for the closest `bun:test` style, and at `src/lib/music-domain.ts` (`parseStoredOctaves`, `expandOctaveRange`, `deriveAvailablePitches`, `deriveEligibleBoundaries`), `src/lib/duration-selection-validator.ts` (`computeOfferableDurations`), and `src/lib/etude-params-repository.ts` (the `EtudeParams` interface) for the helpers and shapes the summary reads.

Create `tests/etude-summary.spec.ts` importing from `bun:test` and the not-yet-existing `src/lib/etude-summary.ts`. Cover `deriveStepSummary(params, step)` where `step` is `'notes' | 'split' | 'review'`: (a) for `'notes'`, the returned model contains exactly the setup answers — measure count, meter, key, the selected octave ranges (the parsed selected octaves expressed as the continuous expanded range lowest-to-highest, per PRD user story 10), and hands — and nothing else; (b) for `'split'`, the model contains everything the notes level shows plus the selected pitches (filtered to the currently available set for the stored key and octaves) and the selected durations (filtered to the offerable durations for the stored meter) — both halves always present together, never pitches alone or durations alone, because the notes step is one coherent prerequisite (cross-cutting contract section 5); (c) for `'review'`, the model contains everything the split level shows plus, when `hand === 'both'` and a boundary applies, the split boundary and each hand's resulting pitch set (lower pitches left, higher pitches right, derived from the stored boundary and the selected pitches); for one-hand mode the review level omits the boundary and hand pitch sets (the "where a boundary applies" qualifier); (d) a stored pitch that is no longer in the available set (e.g. after an upstream key or octave change under Issue 11) is filtered out and not rendered as a live selection; (e) a stored duration that is no longer offerable for the current meter is filtered out; (f) a stored boundary that is no longer between two adjacent selected pitches (stale after an upstream pitch change) is treated as no boundary — the boundary and hand pitch sets are omitted; (g) a null or empty `selectedPitches`/`selectedDurations`/`splitBoundary` yields an empty array or absent field, never a stale or invented value; (h) the function is pure — it does not mutate its argument, throw, or touch the DB. These tests must fail because `src/lib/etude-summary.ts` does not exist yet.

---

### 2. Implement `deriveStepSummary` in `src/lib/etude-summary.ts`

**Type**: GREEN
**Output**: `deriveStepSummary` and the `StepSummary` interface exported from `src/lib/etude-summary.ts` as a pure arrow function. The task-1 tests pass, and the full `tests/etude-summary.spec.ts` suite passes.
**Depends on**: 1

Before writing production code, read and follow the coding standards in `Notes/skills/AGENTS.md` and the project `AGENTS.md` (arrow functions, explicit types, no `any`, braces around all `if`/`while` bodies, Mozilla header comment, `@module` doc). Read `Notes/skills/code-writing/typescript-rules`. Look at `src/lib/music-domain.ts` (`parseStoredOctaves`, `expandOctaveRange`, `deriveAvailablePitches`, `deriveEligibleBoundaries`, the `EligibleBoundary` interface), `src/lib/duration-selection-validator.ts` (`computeOfferableDurations`, `loadRhythmCatalog`), and `src/lib/rhythm-catalog-data.ts` (`RHYTHM_CATALOG_TEXT`) for the pure helpers to reuse — do not re-derive available pitches, offerable durations, or eligible boundaries inline.

Create `src/lib/etude-summary.ts`. Export a typed `StepSummary` interface describing the read-only model (measure count, meter, key, expanded octave range label, hands, and optional selected pitches, selected durations, split boundary, and left/right hand pitch sets), and `deriveStepSummary(params: EtudeParams, step: 'notes' | 'split' | 'review'): StepSummary`. The implementation: (1) always populate the setup-level fields from the committed aggregate — measure count, meter, key, the expanded octave range (parse `selectedOctaves`, expand via `expandOctaveRange`, present as a lowest-to-highest label), and hands; (2) for `'split'` and `'review'`, parse the stored `selectedPitches` into an array, filter it to the currently available set (`deriveAvailablePitches` for the stored key and octaves), and parse the stored `selectedDurations`, filtering to the offerable durations for the stored meter (`computeOfferableDurations`); both halves are always populated together; (3) for `'review'` only, when `hand === 'both'` and a stored boundary exists, derive the eligible boundaries from the filtered selected pitches (`deriveEligibleBoundaries`) and, if the stored boundary matches an eligible boundary's id, populate the boundary and the left/right hand pitch sets from that boundary; if the stored boundary is stale (no match), omit the boundary and hand pitch sets entirely — never invent or display an ineligible boundary; for one-hand mode, omit the boundary and hand pitch sets; (4) the function is pure — it never mutates its argument or throws, and every value comes from the committed aggregate, never from request input (cross-cutting contract section 1). Run the task-1 tests and the full `tests/etude-summary.spec.ts` suite to confirm they pass.

---

### 3. Write failing Playwright e2e tests for the notes-step summary and Back link

**Type**: RED
**Output**: `e2e-tests/etude/21-etude-notes-summary-back-link.spec.ts` that fails because the notes step does not yet render a read-only summary or a Back link. The tests assert the notes step shows the setup answers as read-only text with no editable controls for prior answers, that the summary contains exactly the required fields, that Back is a GET link to the canonical prior step, that unsaved notes edits are discarded after Back, and that an upstream-invalidated value is not rendered.
**Depends on**: 2

Before writing any production code, read and follow the coding standards in `Notes/skills/AGENTS.md` and the project `AGENTS.md` (red-green-refactor, Playwright, `data-testid` kebab-case with `-action` suffix for links). Look at `e2e-tests/etude/16-etude-notes-duration-selection.spec.ts` for the existing notes-step e2e pattern (`testWithDatabase`, `signInUser`, `navigateToHome`, `TEST_USERS`, `SERVER_BASE_URL`, the setup-post helper, the notes-post helper, and the `getAggregateState` test-route helper pattern). Look at `e2e-tests/support/` for available helpers and `e2e-tests/sign-in/` for sign-in examples.

Create `e2e-tests/etude/21-etude-notes-summary-back-link.spec.ts` importing from `@playwright/test` and the support helpers, wrapped in `testWithDatabase`. Sign in as a `KNOWN_USER`, navigate to `/etude` to create the aggregate, and submit a valid setup (e.g. C major, octave 4, right hand, 8 measures, 4/4) so `setupConfirmed` becomes true and the notes step is reached. Cover: (a) the notes step renders a read-only summary of the setup answers as text — measure count, meter, key, selected octave ranges, and hands — each in an element with a `data-testid` like `summary-measures`, `summary-meter`, `summary-key`, `summary-octaves`, `summary-hands`; (b) the summary contains no editable form controls for any prior answer (assert no `input`/`select`/`textarea` exists inside the summary region); (c) the summary values match the saved state (e.g. measures shows "8", meter shows "4/4", key shows "C major", hands shows "right", octaves shows the expanded range); (d) a Back link is present as an anchor (`data-testid='notes-back-action'`) whose `href` is `/etude/setup` and whose method is a GET (assert it is an `<a>`, not a form submit, and that following it lands on the setup step); (e) unsaved edits are discarded — change a notes checkbox without submitting, click Back, then return to the notes step and confirm the unsaved change was not persisted (via `getAggregateState`); (f) an upstream-invalidated value is not rendered — change the key on the setup step (which invalidates the notes selection under Issue 11), return to the notes step, and confirm the summary reflects the new saved setup values rather than the stale notes selection. These tests must fail because the notes step does not yet render a summary or a Back link.

---

### 4. Create `src/components/etude-summary.tsx` and wire it and the Back link into the notes route

**Type**: GREEN
**Output**: `src/components/etude-summary.tsx` exports a shared read-only `EtudeSummary` component driven by `deriveStepSummary`, rendering text only (no form controls) with `data-testid` anchors per field. `src/routes/build-etude-notes.tsx` renders the notes-level summary above the notes form and adds a canonical GET Back link (`<a href={PATHS.ETUDE_SETUP} data-testid='notes-back-action'>`). The task-3 e2e tests pass.
**Depends on**: 2, 3

Before writing production code, read and follow the coding standards in `Notes/skills/AGENTS.md` and the project `AGENTS.md` (arrow functions, explicit types, no `any`, braces around all `if`/`while` bodies, `data-testid` kebab-case with `-action` suffix for links, `redirectWithMessage`/`redirectWithError` from `src/lib/redirects.tsx` — never `c.redirect`). Read `Notes/skills/code-writing/typescript-rules`, `Notes/skills/code-writing/web-behavior`, and `Notes/skills/code-writing/styling-html-and-tsx`. Look at `src/components/error-summary.tsx` for the existing component pattern (a presentational TSX component exported as an arrow function, with `data-testid` anchors and Tailwind classes), and at `src/routes/build-etude-notes.tsx` (`renderEtudeNotesForm` and the `GET` handler) for where the summary and Back link are inserted.

Create `src/components/etude-summary.tsx` exporting `EtudeSummary`, a presentational arrow component that takes the `EtudeParams` snapshot and a `step` of `'notes' | 'split' | 'review'`, calls `deriveStepSummary`, and renders the read-only summary as text only — no `input`, `select`, `textarea`, or `button` elements. Each field is rendered in an element with a stable `data-testid` (`summary-measures`, `summary-meter`, `summary-key`, `summary-octaves`, `summary-hands`, and, when present, `summary-pitches`, `summary-durations`, `summary-split-boundary`, `summary-left-hand-pitches`, `summary-right-hand-pitches`). The component reveals no internal identifiers (no aggregate id, no workflow version, no epoch) and asserts no ownership from request input — every value comes from the passed snapshot (cross-cutting contract section 1). Then modify `src/routes/build-etude-notes.tsx`: in the `GET` handler's render, insert `<EtudeSummary params={result.value} step='notes' />` above the notes form, and add a Back link `<a href={PATHS.ETUDE_SETUP} data-testid='notes-back-action' className='btn btn-ghost'>Back</a>` in the card actions. The Back link is a plain anchor issuing a GET to the canonical prior step (`/etude/setup`); because it is not a form submission, unsaved edits on the notes page are discarded rather than persisted. Run the task-3 e2e tests to confirm they pass.

---

### 5. Write failing Playwright e2e tests for the split-step summary and Back link

**Type**: RED
**Output**: `e2e-tests/etude/22-etude-split-summary-back-link.spec.ts` that fails because the split step does not yet render a read-only summary or a Back link. The tests assert the split step shows the setup and notes answers as read-only text (pitches and durations both present, never one alone), that the summary contains exactly the required fields, that Back is a GET link to `/etude/notes`, and that unsaved split edits are discarded after Back.
**Depends on**: 4

Before writing any production code, read and follow the coding standards in `Notes/skills/AGENTS.md` and the project `AGENTS.md` (red-green-refactor, Playwright, `data-testid` kebab-case with `-action` suffix for links). Look at `e2e-tests/etude/20-etude-split-step.spec.ts` for the existing split-step e2e pattern (`testWithDatabase`, `signInUser`, the setup-post and notes-post helpers, and the `getAggregateState` test-route helper), and at the task-3 test file for the summary-assertion style.

Create `e2e-tests/etude/22-etude-split-summary-back-link.spec.ts` importing from `@playwright/test` and the support helpers, wrapped in `testWithDatabase`. Sign in as a `KNOWN_USER`, navigate to `/etude`, submit a valid setup with `hand: 'both'` and a meter/key that yields several available pitches (e.g. C major, octave 4 → C4..C5), then submit a valid notes selection (at least two pitches and a valid duration set) so the split step is reached. Cover: (a) the split step renders a read-only summary of the setup answers (measure count, meter, key, octave ranges, hands) and the notes answers (selected pitches and selected durations) as text, each with the `data-testid` anchors from task 4; (b) the summary contains no editable form controls for any prior answer; (c) the pitches and durations are both shown together — never pitches alone or durations alone — because the notes step is one coherent prerequisite; (d) the summary values match the saved state (via `getAggregateState`); (e) a Back link is present as an anchor (`data-testid='split-back-action'`) whose `href` is `/etude/notes` and whose method is a GET (assert it is an `<a>`, not a form submit, and that following it lands on the notes step); (f) unsaved split edits are discarded — choose a boundary radio without submitting, click Back, then return to the split step and confirm the unsaved choice was not persisted. These tests must fail because the split step does not yet render a summary or a Back link.

---

### 6. Wire the summary component and Back link into the split route

**Type**: GREEN
**Output**: `src/routes/build-etude-split.tsx` renders the split-level summary above the split form and adds a canonical GET Back link (`<a href={PATHS.ETUDE_NOTES} data-testid='split-back-action'>`). The task-5 e2e tests pass.
**Depends on**: 4, 5

Before writing production code, read and follow the coding standards in `Notes/skills/AGENTS.md` and the project `AGENTS.md` (arrow functions, explicit types, no `any`, braces around all `if`/`while` bodies, `data-testid` kebab-case with `-action` suffix for links). Read `Notes/skills/code-writing/typescript-rules` and `Notes/skills/code-writing/styling-html-and-tsx`. Look at `src/routes/build-etude-split.tsx` (`renderEtudeSplitForm` and the `GET` handler) for where the summary and Back link are inserted, and reuse the `EtudeSummary` component from task 4.

Modify `src/routes/build-etude-split.tsx`: in the `GET` handler's render (the two-hand reachable branch that renders the split form), insert `<EtudeSummary params={params} step='split' />` above the split form, and add a Back link `<a href={PATHS.ETUDE_NOTES} data-testid='split-back-action' className='btn btn-ghost'>Back</a>` in the card actions. The Back link is a plain anchor issuing a GET to the canonical prior step (`/etude/notes`); because it is not a form submission, unsaved edits on the split page are discarded rather than persisted. Do not render the summary or Back link on the one-hand or corrupt-state redirect paths (those branches redirect before rendering). Run the task-5 e2e tests to confirm they pass.

---

### 7. Write failing Playwright e2e tests for the stub review route summary and Back link

**Type**: RED
**Output**: `e2e-tests/etude/23-etude-review-summary-back-link.spec.ts` that fails because `GET /etude/review` does not exist yet. The tests assert the review step renders a read-only summary of every earlier answer plus the split boundary and each hand's pitch set (where a boundary applies), that Back is a GET link to the canonical prior step (split for two-hand, notes for one-hand), that a direct visit when prerequisites are unmet redirects to the canonical route, and that the summary reflects only committed still-valid values.
**Depends on**: 6

Before writing any production code, read and follow the coding standards in `Notes/skills/AGENTS.md` and the project `AGENTS.md` (red-green-refactor, Playwright, `data-testid` kebab-case with `-action` suffix for links). Look at `e2e-tests/etude/20-etude-split-step.spec.ts` and the task-3 and task-5 test files for the summary-assertion style and the two-hand and one-hand flow setup patterns.

Create `e2e-tests/etude/23-etude-review-summary-back-link.spec.ts` importing from `@playwright/test` and the support helpers, wrapped in `testWithDatabase`. Cover two-hand and one-hand flows: (a) two-hand — sign in, submit setup with `hand: 'both'`, confirm notes, confirm a split boundary, then `GET /etude/review` renders a read-only summary containing the setup answers, the notes answers (pitches and durations), the split boundary, and each hand's resulting pitch set (left and right), each with the `data-testid` anchors from task 4, and no editable form controls for any prior answer; (b) the two-hand review Back link is an anchor (`data-testid='review-back-action'`) whose `href` is `/etude/split` and whose method is a GET; (c) one-hand — sign in, submit setup with `hand: 'right'`, confirm notes, then `GET /etude/review` renders a read-only summary containing the setup and notes answers but no split boundary and no hand pitch sets (the "where a boundary applies" qualifier); (d) the one-hand review Back link is an anchor (`data-testid='review-back-action'`) whose `href` is `/etude/notes` and whose method is a GET; (e) a direct `GET /etude/review` when prerequisites are unmet (e.g. notes unconfirmed) redirects to the canonical route (`/etude/notes`) with a safe message; (f) the summary values match the saved state (via `getAggregateState`); (g) an upstream-invalidated value is not rendered (e.g. after a key change that invalidates the notes selection, the review summary reflects the new saved values, and a direct review visit redirects to the earliest incomplete step). These tests must fail because `GET /etude/review` does not exist yet.

---

### 8. Implement the stub `GET /etude/review` route

**Type**: GREEN
**Output**: `src/routes/build-etude-review.tsx` attaches `GET /etude/review` to the app, wired into the app's route registration the same way as the other etude route builders. The stub renders the review-level summary (`EtudeSummary` with `step='review'`) and a canonical GET Back link (to `/etude/split` for two-hand, `/etude/notes` for one-hand). A direct visit when review is not reachable redirects to the canonical route with a safe message. The task-7 e2e tests pass. This is a stub that Issue 19 will replace/extend with the full review step (Generate form, etc.); it renders only the summary and Back link and accepts no POST.
**Depends on**: 6, 7

Before writing production code, read and follow the coding standards in `Notes/skills/AGENTS.md` and the project `AGENTS.md` (arrow functions, explicit types, no `any`, braces around all `if`/`while` bodies, `data-testid` kebab-case with `-action` suffix for links, `redirectWithMessage`/`redirectWithError` from `src/lib/redirects.tsx` — never `c.redirect`). Read `Notes/skills/code-writing/typescript-rules` and `Notes/skills/code-writing/styling-html-and-tsx`. Look at `src/routes/build-etude-split.tsx` and `src/routes/build-etude-notes.tsx` for the closest existing route pattern: the `signedInAccess` middleware, `secureHeaders(STANDARD_SECURE_HEADERS)` for the GET (no error-summary focus script is needed because the stub renders no form), `loadEtudeParams`/`resolveCanonicalRoute`, the `handleUnexpectedError`/`logError`/`sanitizeError` error handling, and how the route builder is registered in the app entry so `build-etude-review.tsx` is registered the same way.

Create `src/routes/build-etude-review.tsx`. The `GET` handler: (1) require `user`/`db`, else redirect to sign-in; (2) `loadEtudeParams`; on error, unexpected-error path; null → redirect to `/etude`; (3) if `resolveCanonicalRoute(params) !== PATHS.ETUDE_REVIEW`, redirect to `resolveCanonicalRoute(params)` with a safe message — this single guard covers setup-unconfirmed, notes-unconfirmed, two-hand-split-unconfirmed, and the corrupt-state fewer-than-two-pitches rows (cross-cutting contract section 5: any direct GET for a step that is not reachable redirects to the canonical route); (4) otherwise render the review-level summary via `<EtudeSummary params={params} step='review' />` inside the standard layout, with a Back link whose target is the canonical prior step — `/etude/split` when `params.hand === 'both'`, `/etude/notes` otherwise — as a plain anchor `data-testid='review-back-action'` issuing a GET. The stub defines no `POST` handler and no Generate form; Issue 19 will replace this stub with the full review step. The route inherits cross-cutting contract section 1 (auth + no-cache via `signedInAccess`, owner-scoped, no internal identifiers revealed). Register the route builder in the app entry alongside `buildEtude`, `buildEtudeNotes`, and `buildEtudeSplit`. Run the task-7 e2e tests to confirm they pass.

---

### 9. Refactor and full test run

**Type**: REFACTOR
**Output**: No duplicated summary logic between `src/lib/`, `src/components/`, and the routes; the summary model lives only in `src/lib/etude-summary.ts`, the rendering only in `src/components/etude-summary.tsx`, and each route only calls `EtudeSummary` with the right `step`; no route inlines available-pitch/offerable-duration/eligible-boundary derivation for the summary; `tsc --noEmit` reports zero errors in any file created or modified for this issue; the full `bun test` suite and the `npx playwright test` suite pass.
**Depends on**: 8

Read and follow the coding standards in `Notes/skills/AGENTS.md` and the project `AGENTS.md`. Search `src/routes/`, `src/lib/`, `src/components/`, and `tests/` for any duplicated summary logic, inline available-pitch/offerable-duration/eligible-boundary computation that should call the existing `src/lib/` helpers, or inline summary rendering that should call `EtudeSummary`. Move any such logic behind `src/lib/etude-summary.ts` or `src/components/etude-summary.tsx`. Confirm the Back links are plain anchors (not form submits) in every route. Run `tsc --noEmit`, the full `bun test` suite, and `npx playwright test` and confirm they are green. Do not modify the parent issue, the parent PRD, or prior task/issue/walkthrough files in `Notes/`.

---

### 10. Update wiki and notes documentation

**Type**: DOCUMENT
**Output**: Wiki and Notes updates describing the read-only summaries and Back links: the `deriveStepSummary` pure function in `src/lib/etude-summary.ts` (notes/split/review levels, filtering of invalidated/stale values, the notes step as one coherent pitches-and-durations prerequisite), the shared `EtudeSummary` component in `src/components/etude-summary.tsx` (text-only, no editable controls, no internal identifiers revealed), the Back-link wiring on the notes and split steps (canonical GET links to `/etude/setup` and `/etude/notes`), and the stub `GET /etude/review` route in `src/routes/build-etude-review.tsx` (review-level summary, canonical prior-step Back link, unreachable-direct-visit redirect, no POST). Follow `Notes/wiki/wiki-rules.md` and `Notes/wiki/AGENTS.md` for the wiki ingest operation.
**Depends on**: 9

Before writing documentation, read `Notes/wiki/AGENTS.md` and `Notes/wiki/wiki-rules.md` for the wiki conventions (ingest operation, kebab-case filenames, update `index.md` and append to `log.md` with the `## [YYYY-MM-DD] <operation> | <subject>` format).

Update the relevant wiki pages: `Notes/wiki/source-code.md` (add `src/lib/etude-summary.ts`, `src/components/etude-summary.tsx`, and `src/routes/build-etude-review.tsx`, and note the Back-link additions to `src/routes/build-etude-notes.tsx` and `src/routes/build-etude-split.tsx`), `Notes/wiki/e2e-tests.md` (catalog `e2e-tests/etude/21-etude-notes-summary-back-link.spec.ts`, `22-etude-split-summary-back-link.spec.ts`, and `23-etude-review-summary-back-link.spec.ts`), `Notes/wiki/unit-tests.md` (catalog `tests/etude-summary.spec.ts`), `Notes/wiki/project-overview.md` (describe the read-only summaries on later steps, the shared summary component driven by the workflow snapshot, the canonical GET Back links that discard unsaved edits, the notes step as one coherent prerequisite, the filtering of invalidated values, and the stub review route that Issue 19 will replace), and `Notes/wiki/index.md` if new sections are added. Append a `## [YYYY-MM-DD] ingest | issue-017 read-only summaries and back links` entry to `Notes/wiki/log.md`. Do not modify the parent issue or the parent PRD.

---

### 11. Code walkthrough

**Type**: CODE WALKTHROUGH
**Output**: `Notes/walkthroughs/issue-017-read-only-summaries-back-links/code-walkthrough/` containing the files generated by `uvx showboat` for this implementation.
**Depends on**: 10

Run `uvx showboat --help` for current usage, then generate a walkthrough of the issue-017 implementation into a new directory `Notes/walkthroughs/issue-017-read-only-summaries-back-links/code-walkthrough/`. The walkthrough should cover: (1) the `deriveStepSummary` pure function in `src/lib/etude-summary.ts` (notes/split/review levels, filtering of invalidated/stale pitches, durations, and boundaries, the notes step as one coherent prerequisite, the review-level boundary and hand pitch sets where a boundary applies), (2) the shared `EtudeSummary` component in `src/components/etude-summary.tsx` (text-only rendering, no editable controls, no internal identifiers revealed), (3) the Back-link wiring in `src/routes/build-etude-notes.tsx` and `src/routes/build-etude-split.tsx` (canonical GET links to `/etude/setup` and `/etude/notes`, unsaved edits discarded), and (4) the stub `GET /etude/review` route in `src/routes/build-etude-review.tsx` (review-level summary, canonical prior-step Back link, unreachable-direct-visit redirect). Place all generated files there.

---

### 12. Human review against the PRD and cross-cutting contract

**Type**: REVIEW
**Output**: A human verifies the implementation against the issue's "How to verify" and "Acceptance criteria" sections, the PRD's user stories 23 and 24, and cross-cutting contract §1 (the summary reveals no internal identifiers and asserts no ownership from request input; every value comes from the owner-scoped aggregate) and §5 (Back links target the canonical prior step, and the notes step is treated as a single pitches-and-durations prerequisite).
**Depends on**: 11

This is a human-in-the-loop step. The human must verify: (a) given any later step (notes, split, review), prior answers are rendered as read-only text and not as editable controls; (b) given the notes step, its summary shows measure count, meter, key, selected octave ranges, and hands; (c) given the split step, its summary additionally shows the selected pitches and the selected durations, both halves together; (d) given the review step, its summary shows every earlier answer plus the split boundary and each hand's pitch set where a boundary applies (and omits them for one-hand mode); (e) given a value that is currently invalid or has been invalidated by an upstream change, no summary renders it — a summary reflects only the committed, still-valid aggregate; (f) given a change to a prior step, when a later step is loaded, its summary reflects the new saved values; (g) given unsaved edits on a later step, when Back is followed, those edits are not persisted; (h) given Back on any later step, it is a link issuing a GET to the canonical prior step (notes → `/etude/setup`, split → `/etude/notes`, review → `/etude/split` for two-hand or `/etude/notes` for one-hand); (i) the stub review route redirects a direct unreachable visit to the canonical route and defines no POST (Issue 19 will replace it with the full review step). Record the result in the review notes. Do not modify the parent issue or the parent PRD.

---
