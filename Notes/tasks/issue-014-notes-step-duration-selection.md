# Tasks for #14: Notes step duration selection with compatible defaults and server rhythm validation

Parent issue: #14
Parent PRD: `Notes/PRD-etude-generator.md`

## Tasks

### 1. Write failing Bun tests for duration-selection module

**Type**: RED
**Output**: A failing `tests/duration-selection-validator.spec.ts` that asserts four pure functions: `computeOfferableDurations` (the offerable duration set per meter), `validateDurationSelection` (duplicate de-dup, canonical ordering, unknown/not-offerable rejection, empty rejection, eligibility with correction), `computeCorrectiveSuggestion` (the smallest set of additional offered durations that restores eligibility, for several meter/selection combos), and `resolveDurationSelectionState` (all-offerable first-derivation default, non-re-expansion of a stored narrowed selection, re-derivation after an Issue 11 clear).
**Depends on**: none

Before writing any production code, read and follow the coding standards in `Notes/skills/AGENTS.md` and the project `AGENTS.md` (red-green-refactor, `bun:test` with `describe`/`it`/`expect`, arrow functions, explicit types, no `any`, kebab-case filenames, braces around all `if`/`while` bodies). Read `Notes/skills/code-writing/typescript-rules` for the TypeScript conventions. Look at `tests/pitch-selection-validator.spec.ts` for the closest existing `bun:test` style (the `unwrap`/`unwrapErr` helpers, the `Result` import from `true-myth/result`, the header comment block) and `tests/rhythm-catalog-eligible.spec.ts` for how a test builds a catalog from `parseRhythmCatalog` with a small synthetic text and imports `computeEligibleRhythms`.

Create `tests/duration-selection-validator.spec.ts` importing from `bun:test`, `true-myth/result`, and the not-yet-existing `src/lib/duration-selection-validator.ts`, plus `parseRhythmCatalog`/`TOKEN_DURATIONS` from `src/lib/rhythm-catalog.ts`. Build a small synthetic catalog that validates and that keeps the compatibility cases tractable — clearly mappings of which duration tokens appear in each meter's patterns (e.g. a 2/4 meter whose only patterns are `QQ`, `EEEE`, `ER`, `H`, and a 3/4 meter whose only pattern is `D`), so offerability and eligibility are predictable.

Cover `computeOfferableDurations(catalog, meter): string[]`: (a) for a meter, returns exactly the distinct duration tokens (in the canonical order used by the module) that appear in at least one pattern of that meter; (b) a token present in one meter's patterns but absent from another meter's patterns is offerable for the former and not for the latter; (c) the result contains no duplicates and is filtered to supported tokens only. Cover `validateDurationSelection(submitted: unknown, catalog, meter): Result<string[], DurationSelectionFailure[]>`: (d) a non-array submitted value (null, undefined, a string, a number) → `Result.err` with a field-addressable failure on `'durations'` with an empty-selection message; (e) an empty or all-whitespace set → `Result.err` with the empty-selection message; (f) duplicate submissions of the same offerable token → de-duplicated and accepted as a single entry; (g) a reordered submission → accepted with the stored set normalized to canonical order; (h) an unknown or unsupported token → `Result.err` with a field-addressable failure naming the offending token, nothing accepted; (i) a supported token that is not offerable for the current meter → `Result.err` with a field-addressable failure naming the token and the meter; (j) a set with at least one eligible complete-measure pattern (the eligible calculation per the catalog) → `Result.ok` with the tokens in canonical order; (k) a set of only-offerable tokens with no eligible pattern → `Result.err` with a single group-level failure on `'durations'` whose message names the duration group, states that no complete measure can be built for the current meter, and names the computed corrective suggestion (assert the exact stable message string for this specific combination); (l) the eligibility check is authoritative — a set built entirely from offerable tokens is rejected when the catalog has no eligible pattern, never a 500; (m) the function is pure — it does not mutate its arguments, touch the DB, or throw for any input. Cover `computeCorrectiveSuggestion(catalog, meter, selectedTokens): string[]`: (n) for a selection with no eligible pattern, returns the smallest set of additional offered durations (in canonical order) whose addition makes at least one pattern eligible; (o) for at least three distinct meter/selection combinations, asserts the exact expected suggestion (for example a 2/4 case where adding the eighth restores an `ER`-style pattern, a 3/4 case where adding dotted-half restores a single-token `D` pattern, and a multi-token-impossible case) — the assertions must not depend on catalog pattern text or token letters in the message, only on the suggestion values; (p) when the selection already has an eligible pattern, returns an empty suggestion (it is not called in that path, but the function's contract is deterministic and pure). Cover `resolveDurationSelectionState(storedDurations: string | null, offerableDurations: string[]): { selectedDurations: string[]; isFirstDerivation: boolean }`: (q) `storedDurations: null` → `{ selectedDurations: [...offerableDurations], isFirstDerivation: true }` (first derivation preselects every offerable duration); (r) `storedDurations: ''` (empty string) → treated as null → first derivation with all offerable preselected; (s) a stored narrowed set → returned as-is, ordered by offerable order, `isFirstDerivation: false`, never re-expanded; (t) a stored token no longer offerable for the current meter → filtered out (offerable ones retained), `isFirstDerivation: false`; (u) the function is pure — it does not mutate its arguments or throw. These tests must fail because the module does not exist yet.

---

### 2. Implement duration-selection module

**Type**: GREEN
**Output**: `src/lib/duration-selection-validator.ts` exports the `DurationSelectionFailure` interface, `DURATION_LABELS`, `CANONICAL_DURATION_ORDER`, `EMPTY_DURATION_MESSAGE`, `computeOfferableDurations`, `computeCorrectiveSuggestion`, `validateDurationSelection`, and `resolveDurationSelectionState` as pure arrow functions. The task-1 tests pass.
**Depends on**: 1

Before writing production code, read and follow the coding standards in `Notes/skills/AGENTS.md` and the project `AGENTS.md` (arrow functions, explicit types, no `any`, braces around all `if`/`while` bodies, one export per file where practical, `Result` from `true-myth/result`, Mozilla header comment, `@module` doc, typed failure interfaces, field-addressable failure shape). Read `Notes/skills/code-writing/typescript-rules`. Look at `src/lib/pitch-selection-validator.ts` for the pure-module style (the `normalizeSubmitted` helper, the availability filter, the deterministic de-dup, canonical ordering, and `resolveXState` first-derivation semantics) and `src/lib/rhythm-catalog.ts` for `computeEligibleRhythms`, `SUPPORTED_TOKENS`, and `SUPPORTED_METERS`.

Create `src/lib/duration-selection-validator.ts`. Export:
- `DURATION_LABELS: Readonly<Record<string, string>>` mapping each token to its human label — `W` 'whole', `H` 'half', `D` 'dotted half', `Q` 'quarter', `R` 'dotted quarter', `E` 'eighth' — used for displayed control text and for the corrective message. The corrective message must expose display labels only, never token letters.
- `CANONICAL_DURATION_ORDER: readonly string[]` — a fixed token ordering (descending duration, e.g. `W, H, D, Q, R, E`) used both to order the offerable list and to normalize the stored set to canonical order regardless of submission order.
- `EMPTY_DURATION_MESSAGE` — the exact stable string for an empty selection, e.g. `"Select at least one duration."`
- `DurationSelectionFailure` (`{ field: 'durations'; reason: string }`).
- `computeOfferableDurations(catalog: RhythmCatalog, meter: string): string[]` — collects every token appearing in any pattern of `catalog.meters[meter]` and returns them filtered to `CANONICAL_DURATION_ORDER` order; returns `[]` for an unsupported meter.
- `computeCorrectiveSuggestion(catalog: RhythmCatalog, meter: string, selectedTokens: ReadonlySet<string>): string[]` — finds the smallest addition set of offered durations not already selected (searching k = 1 upward, trying each canonical-ordered combination) that makes `computeEligibleRhythms` non-empty, and returns that addition set in canonical order. The search must be deterministic.
- `validateDurationSelection(submitted: unknown, catalog: RhythmCatalog, meter: string): Result<string[], DurationSelectionFailure[]>` — (1) normalizes `submitted` to a trimmed `string[]` via a `normalizeSubmitted` helper (a non-array yields an empty array); (2) deduplicates; (3) rejects each token that is not a supported token (field-addressable failure naming the offending token) and each supported token not in `computeOfferableDurations` for the meter (field-addressable failure naming the token and the meter); (4) if the kept (offerable) set is empty, emits a failure with `EMPTY_DURATION_MESSAGE`; (5) if the kept set is non-empty but `computeEligibleRhythms(catalog, meter, keptSet)` is empty, emits a single group-level failure on `'durations'` whose message (a stable string built by a `buildImpossibleSetMessage(meter, suggestionTokens)` helper) names the duration group, states that no complete measure can be built from the selected durations for the current meter, and names the computed corrective suggestion using display labels from `DURATION_LABELS` (e.g. `"the eighth duration"` or `"the half and quarter durations"`); the message must never enumerate catalog patterns, internal token letters, or line numbers; (6) on success returns `Result.ok` with the kept tokens ordered by `CANONICAL_DURATION_ORDER` (not submission order); (7) is pure — never throws, never mutates. Fix the exact message wording in this module. Export `resolveDurationSelectionState(storedDurations: string | null, offerableDurations: string[]): { selectedDurations: string[]; isFirstDerivation: boolean }` — when `storedDurations` is null or empty/whitespace, returns `{ selectedDurations: [...offerableDurations], isFirstDerivation: true }`; otherwise parses the comma-separated stored string, filters to tokens still in `offerableDurations` (dropping tokens no longer offerable after an upstream meter change), orders them by `CANONICAL_DURATION_ORDER`, and returns `isFirstDerivation: false`. Run the task-1 tests to confirm they pass.

---

### 3. Write failing Bun tests for `updateEtudeNotes` repository function

**Type**: RED
**Output**: New cases in `tests/etude-params-repository.spec.ts` that fail because `updateEtudeNotes` does not exist yet. The tests assert the combined CAS persistence of `selectedPitches` and `selectedDurations`, the `notesConfirmed: true` transition, the workflow-version increment, the stale-version and epoch-mismatch rejections, the identical-resubmit short-circuit, the db-error handling, and that downstream state is untouched.
**Depends on**: 2

Before writing any production code, read and follow the coding standards in `Notes/skills/AGENTS.md` and the project `AGENTS.md` (red-green-refactor, `bun:test`, arrow functions, explicit types, no `any`, kebab-case filenames). Look at `tests/etude-params-repository.spec.ts` for the existing repository test pattern (`createTestDb`, `insertUser`, `unwrap`, `loadOrCreateEtudeParams`, `loadEtudeParams`, the `validSetup` constant, the `confirmSetup` helper, and the existing `updateEtudePitches` `describe` block). Read `Notes/skills/code-writing/typescript-rules` and `Notes/skills/code-writing/database-access`.

Add a `describe` block for `updateEtudeNotes`, importing it from `src/lib/etude-params-repository.ts`: (a) a combined save with valid `expectedWorkflowVersion` and `expectedEpoch`, `selectedPitches: ['C4','D4']`, `selectedDurations: ['E','Q']` → `Result.ok` with `selectedPitches` `'C4,D4'`, `selectedDurations` stored in canonical order, `workflowVersion` incremented by 1, `notesConfirmed: true` (both halves confirmed in this commit), `setupConfirmed` still true, and `splitBoundary`/`splitConfirmed` unchanged; (b) a combined save where the submission matches the stored pitches but changes durations (or vice versa) → the changed field updates, version increments, `notesConfirmed: true`; (c) a stale `expectedWorkflowVersion` (stored - 1) → `Result.err` with `kind: 'version-mismatch'`, and a reload confirms neither `selectedPitches` nor `selectedDurations` changed and `notesConfirmed` is unchanged; (d) a stale `expectedEpoch` (stored - 1) → `Result.err` with `kind: 'epoch-mismatch'`, nothing changed on reload; (e) an injected failure during the write — pass a `DrizzleClient` wrapper whose `update` throws, assert `result.isErr` and `error.kind === 'db-error'`, and confirm via a separate real `createTestDb` client that nothing was persisted; (f) an identical resubmit (same pitches and durations as stored, same version) → `Result.ok` with no version increment and `notesConfirmed` unchanged (matching the identical-resubmit short-circuit pattern); (g) a stale-version resubmit of identical values → still `Result.err` `version-mismatch`. These tests must fail because `updateEtudeNotes` does not exist yet.

---

### 4. Implement `updateEtudeNotes`

**Type**: GREEN
**Output**: `updateEtudeNotes` in `src/lib/etude-params-repository.ts` conditionally sets `selectedPitches`, `selectedDurations`, and `notesConfirmed: true` and increments `workflowVersion` via a compare-and-set write matching the `updateEtudeSetup`/`updateEtudePitches` CAS pattern. The task-3 tests pass, and the full repository suite passes.
**Depends on**: 3

Before writing production code, read and follow the coding standards in `Notes/skills/AGENTS.md` and the project `AGENTS.md` (arrow functions, explicit types, no `any`, braces around all `if`/`while` bodies, `Result` from `true-myth/result`). Read `Notes/skills/code-writing/database-access` and `Notes/skills/code-writing/typescript-rules`. Look at the existing `updateEtudePitches`/`updateEtudePitchesActual` in `src/lib/etude-params-repository.ts` (around lines 391-470) for the exact CAS pattern to follow: load → identical-resubmit short-circuit → conditional `update(...).set(...).where(eq userId, eq aggregateEpoch, eq workflowVersion).returning()` → zero-row disambiguation.

Add `updateEtudeNotes` and `updateEtudeNotesActual` to `src/lib/etude-params-repository.ts`, following the `updateEtudePitches` CAS pattern. The signature: `updateEtudeNotes(db, userId, expectedEpoch, expectedWorkflowVersion, selectedPitches: string[], selectedDurations: string[]): Promise<Result<EtudeParams, EtudeUpdateError>>`. The implementation: (1) load the current row; if none, return `Result.err({ kind: 'version-mismatch' })`; (2) compare the submitted `selectedPitches.join(',')` and `selectedDurations.join(',')` against the stored `selectedPitches`/`selectedDurations` — if both identical and the version matches, return `Result.ok` with no write (identical-resubmit short-circuit); if identical but the version is stale, return `Result.err({ kind: 'version-mismatch' })`; (3) otherwise issue a conditional `update(...).set({ selectedPitches: selectedPitches.join(','), selectedDurations: selectedDurations.join(','), notesConfirmed: true, workflowVersion: sql`${etudeParams.workflowVersion} + 1`, updatedAt: new Date() }).where(eq userId, eq aggregateEpoch, eq workflowVersion).returning()`; (4) on zero rows, re-load and disambiguate `epoch-mismatch` vs `version-mismatch` exactly as `updateEtudePitchesActual` does; (5) on a thrown error, return `Result.err({ kind: 'db-error', error })`. This combined save commits both halves of the coherent notes-step prerequisite in one transition, so `notesConfirmed` becomes `true`. Do NOT modify `splitBoundary` or `splitConfirmed`. Keep `updateEtudePitches` unchanged (it remains the pitch-only path for the Select all action). Run the task-3 tests and the full repository suite to confirm they pass.

---

### 5. Write failing Playwright e2e tests for duration selection

**Type**: RED
**Output**: `e2e-tests/etude/16-etude-notes-duration-selection.spec.ts` that fails because the notes route does not yet offer duration controls. The tests cover the offered controls per meter, the default all-offerable state, a direct POST of an impossible set producing a group-level error with no persistence and the selection redisplayed, the focused error summary, the inherited stale-version rejection, and that the step becomes complete only after both pitches and durations are confirmed.
**Depends on**: 4

Before writing any production code, read and follow the coding standards in `Notes/skills/AGENTS.md` and the project `AGENTS.md` (red-green-refactor, Playwright, `data-testid` kebab-case with `-action` suffix for submit buttons). Look at `e2e-tests/etude/15-etude-notes-pitch-selection.spec.ts` for the existing notes-step e2e pattern (`testWithDatabase`, `signInUser`, `navigateToHome`, `TEST_USERS`, `SERVER_BASE_URL`, the setup-post helper, and the `getAggregateState` test-route helper pattern). Look at `e2e-tests/etude/10-etude-setup-invalid-redisplay.spec.ts` for the invalid-redisplay pattern and `e2e-tests/etude/11-etude-setup-error-summary.spec.ts` for the error-summary focus and field-error wiring. Look at `e2e-tests/support/` for available helpers and `e2e-tests/sign-in/` for sign-in examples.

Create `e2e-tests/etude/16-etude-notes-duration-selection.spec.ts` importing from `@playwright/test` and the support helpers, wrapped in `testWithDatabase`. Sign in as a `KNOWN_USER`, navigate to `/etude` to create the aggregate, and submit a valid setup that confirms the desired meter (e.g. 4/4 or a 2/4 case) via the setup form so `setupConfirmed` becomes true, then navigate to `/etude/notes`. Cover: (a) offered controls per meter — for the saved meter, exactly the offerable duration checkboxes appear (e.g. for 2/4 the whole and dotted-half controls are absent) with `data-testid` like `duration-field-<token>`; (b) default state — on a freshly derived notes step every offerable duration checkbox is checked (all-compatible default) while pitches also default; (c) a direct POST of an impossible set — post to `/etude/notes` a `workflowVersion` and `durations` set that is entirely offerable but has no eligible pattern (along with a valid pitch set), assert a 303 redirect back to `/etude/notes`, the group-level duration error appears and its message names the duration group and the current meter and at least one concrete corrective duration by display label, the error summary is present and focused and links into the duration group, and nothing was persisted (reload and confirm the stored `selectedDurations` is unchanged and `notesConfirmed` stays false); (d) redisplay of the submitted duration selection alongside the error — the rejected durations are shown checked, the rest unchecked; (e) duplicate submission — submit the same offerable duration value more than once (repeat the field) and confirm it is accepted and persisted de-duplicated in canonical order; (f) an unknown duration token and a token not offerable for the meter — post directly and confirm each is rejected field-addressably with no persistence (never a 500); (g) empty duration selection — deselect all durations and submit, confirm rejection with the empty-selection message and no persistence; (h) stale-version rejection — capture the form's `workflowVersion`, post a valid duration set with a stale version, assert an explanatory error and that the currently saved selection is shown; (i) step completeness — confirm the notes step transitions to complete (canonical route no longer returns `/etude/notes`) only after both pitches and durations are confirmed — e.g. save pitches alone via Select all and reload (still on `/etude/notes`), then submit the combined form with durations, and assert the step is now complete; given only one of the two confirmed, the step remains the earliest incomplete step. These tests must fail because the notes route does not yet offer or validate duration controls.

---

### 6. Extend `GET /etude/notes` and `POST /etude/notes` with durations

**Type**: GREEN
**Output**: `GET /etude/notes` renders a second `durations` fieldset (offerable durations derived from the stored meter's catalog patterns, pre-selected per first-derivation or stored selection, display labels, hidden `workflowVersion`, accessible labels, error-summary wiring for both groups). `POST /etude/notes` ordinary save validates both pitches and durations, persists both via `updateEtudeNotes` (CAS) setting `notesConfirmed: true`, and redirects 303. Select all keeps the pitch-only behavior via `updateEtudePitches`. The task-5 e2e tests pass.
**Depends on**: 4, 5

Before writing production code, read and follow the coding standards in `Notes/skills/AGENTS.md` and the project `AGENTS.md` (arrow functions, explicit types, no `any`, braces around all `if`/`while` bodies, `Result` from `true-myth/result`, `data-testid` kebab-case with `-action` suffix, `value` attribute for form inputs, `redirectWithMessage`/`redirectWithError` from `src/lib/redirects.tsx` — never `c.redirect`). Read `Notes/skills/code-writing/typescript-rules`, `Notes/skills/code-writing/web-behavior`, and `Notes/skills/code-writing/styling-html-and-tsx`. Look at `src/routes/build-etude-notes.tsx` for the existing notes form/route structure: `NOTES_FIELD_ORDER`, `RedisplayData`, `entriesForField`/`describedByFor`/`renderFieldErrors`, `renderEtudeNotesForm`, and the GET/POST handlers.

Extend `src/routes/build-etude-notes.tsx` (the existing notes module from Issue 13) rather than creating a new route file, to keep the coherent notes step in one place. Changes:
- Import the duration functions from `src/lib/duration-selection-validator.ts` (`computeOfferableDurations`, `validateDurationSelection`, `resolveDurationSelectionState`, `DURATION_LABELS`, `CANONICAL_DURATION_ORDER`), `updateEtudeNotes` from `src/lib/etude-params-repository.ts`, and a parsed catalog. Parse the rhythm catalog once per request via `parseRhythmCatalog(RHYTHM_CATALOG_TEXT)` (from `src/lib/rhythm-catalog.ts` / `src/lib/rhythm-catalog-data.ts`) — if the catalog parse can fail, treat a parse failure defensively (an offerable list of `[]` and a rejection path) rather than a 500, following the safe-error conventions.
- Update the `EtudeParamsLike` interface to read `selectedDurations` and `timeSignature`.
- Change `NOTES_FIELD_ORDER` to `['pitches', 'durations']` and set `NOTES_GROUP_FIELDS` to map both groups to their first member ids (`pitches` → first pitch checkbox, `durations` → first offerable duration checkbox) for `buildErrorSummaryEntries`.
- In `renderEtudeNotesForm`, derive `offerableDurations = computeOfferableDurations(catalog, params.timeSignature)`; render a second `<fieldset>` for the durations group alongside the pitches fieldset — one checkbox per offerable duration with `name='durations'`, `value=<token>`, `id`/`data-testid` `duration-field-<token>`, an accessible `<label>` using the `DURATION_LABELS` display label (never the token letter), `aria-describedby` wiring into the duration group instructions and duration errors, and native HTML constraints; resolve the checked state via `resolveDurationSelectionState(params.selectedDurations, offerableDurations)`; when redisplay data is present, override with the safe submitted durations (the submitted tokens shown checked, the rest unchecked). Add a `data-testid` intro text for the offerable range. Keep the existing pitch fieldset, Select all button (pitch-only, unchanged), and the ordinary Save button (`data-testid='notes-save-action'`), which now submits both fieldsets.
- In the `GET` handler, after confirming `setupConfirmed`, parse the catalog and compute `offerableDurations` for the stored meter and pass it through to the form along with the resolved pitch and duration state; consume the validation-state record for redisplay of both pitch and duration safe values.
- In the `POST` handler ordinary-save path (action `'save'`): (1) derive available pitches and offerable durations from the stored key/octave/meter; (2) read submitted `pitches` and `durations` via `form.getAll`; (3) validate pitches with the existing `validatePitchSelection` and durations with `validateDurationSelection`; (4) if either validation fails, shape a single redisplay payload via `shapeRedisplayPayload` carrying both `pitches` and `durations` safe values and all field errors (pitch and duration), and redirect with validation state — durations rejection must produce a group-level field-addressable error on `'durations'` with the corrective message, the submitted duration selection redisplayed, and the error summary focused and linked into the duration group; nothing is persisted; (5) on success call `updateEtudeNotes(db, user.id, params.aggregateEpoch, versionParseResult.value, validatedPitches, validatedDurations)` — a combined CAS save that persists both halves and sets `notesConfirmed: true`; (6) handle conflicts exactly as the existing handler does (`version-mismatch`/`epoch-mismatch` → redirect with an explanatory error showing the newly current saved state; `db-error` → unexpected-error path); (7) on success redirect 303 to `/etude/notes` via `redirectWithMessage`. The Select all branch is unchanged: it persists all available pitches via `updateEtudePitches` and never touches durations or `notesConfirmed` (so the step stays incomplete until a full combined save). Run the task-5 e2e tests to confirm they pass, along with the existing `15-etude-notes-pitch-selection.spec.ts` to confirm the pitch behavior is preserved.

---

### 7. Refactor and full test run

**Type**: REFACTOR
**Output**: No duplicated duration logic between `src/lib/`, `src/routes/`, and `tests/`; the duration validation, offerability, corrective-suggestion, and resolution logic lives only in `src/lib/duration-selection-validator.ts`; the combined CAS pattern is not duplicated inline in the route; `tsc --noEmit` reports zero errors in any file created or modified for this issue; the full `bun test` suite and the `npx playwright test` suite pass.
**Depends on**: 6

Read and follow the coding standards in `Notes/skills/AGENTS.md` and the project `AGENTS.md`. Search `src/routes/`, `src/lib/`, `src/components/`, and `tests/` for any duplicated duration logic, inline offerable-duration or eligibility computation that should call `computeOfferableDurations`/`validateDurationSelection`/`computeCorrectiveSuggestion`, inline corrective-message construction that should use the module's stable message helper, or inline canonical-ordering logic. Move any such logic behind the appropriate `src/lib/` module. Confirm the `updateEtudePitches`/`updateEtudeNotes` CAS pattern is not duplicated inline in the route. If the notes route and the module both need a parsed catalog, share a single helper (e.g. export a cached `parseRhythmCatalog(RHYTHM_CATALOG_TEXT)` from `src/lib/rhythm-catalog.ts` or a small shared function) rather than duplicating the parse. Run `tsc --noEmit`, the full `bun test` suite, and `npx playwright test` and confirm they are green. Do not modify the parent issue, the parent PRD, or prior task/issue/walkthrough files in `Notes/`.

---

### 8. Update wiki and notes documentation

**Type**: DOCUMENT
**Output**: Wiki and Notes updates describing the notes-step duration selection, the `validateDurationSelection`/`computeOfferableDurations`/`computeCorrectiveSuggestion`/`resolveDurationSelectionState` pure functions, the offerable-per-meter semantics, the all-compatible default, the canonical stored ordering, the eligibility rule (at least one eligible complete-measure pattern), the corrective message semantics (duration group, current meter, computed suggestion by display label, no patterns/token letters/line numbers), the combined `updateEtudeNotes` repository function (sets `notesConfirmed: true`), the pitch-only `updateEtudePitches` retained for Select all, and the new test files. Follow `Notes/wiki/wiki-rules.md` and `Notes/wiki/AGENTS.md` for the wiki ingest operation.
**Depends on**: 7

Before writing documentation, read `Notes/wiki/AGENTS.md` and `Notes/wiki/wiki-rules.md` for the wiki conventions (ingest operation, kebab-case filenames, update `index.md` and append to `log.md` with the `## [YYYY-MM-DD] <operation> | <subject>` format).

Update the relevant wiki pages: `Notes/wiki/source-code.md` (add `src/lib/duration-selection-validator.ts`, the `updateEtudeNotes` function in `src/lib/etude-params-repository.ts`, and the extended notes route handlers in `src/routes/build-etude-notes.tsx`), `Notes/wiki/e2e-tests.md` (catalog `e2e-tests/etude/16-etude-notes-duration-selection.spec.ts`), `Notes/wiki/unit-tests.md` (catalog the new `tests/duration-selection-validator.spec.ts` and the new repository test cases), `Notes/wiki/project-overview.md` (describe the notes-step duration selection: offerable durations derived from the stored meter's catalog patterns, the all-compatible default, the non-re-expansion of a stored narrowed selection, the eligibility validation with corrective guidance, the canonical stored ordering, and the combined `updateEtudeNotes` save that confirms the coherent notes step), and `Notes/wiki/index.md` if new sections are added. Append a `## [YYYY-MM-DD] ingest | issue-014 notes step duration selection` entry to `Notes/wiki/log.md`. Do not modify the parent issue or the parent PRD.

---

### 9. Code walkthrough

**Type**: CODE WALKTHROUGH
**Output**: `Notes/walkthroughs/issue-014-notes-step-duration-selection/code-walkthrough/` containing the files generated by `uvx showboat` for this implementation.
**Depends on**: 8

Run `uvx showboat --help` for current usage, then generate a walkthrough of the issue-014 implementation into a new directory `Notes/walkthroughs/issue-014-notes-step-duration-selection/code-walkthrough/`. The walkthrough should cover: (1) the `computeOfferableDurations` pure function (offerable set per meter from the catalog), (2) the `validateDurationSelection` pure function (de-dup, canonical ordering, unknown/not-offerable rejection, empty rejection, eligibility with the corrective message), (3) the `computeCorrectiveSuggestion` pure function (smallest addition set that restores eligibility, named by display label), (4) the `resolveDurationSelectionState` pure function (all-offerable first-derivation default, non-re-expansion, re-derivation after an Issue 11 meter-change clear), (5) the combined `updateEtudeNotes` repository function (CAS persistence of both halves, `notesConfirmed: true`, version increment, identical-resubmit short-circuit, stale-version and epoch-mismatch rejections), and (6) the extended `GET`/`POST /etude/notes` handlers (duration controls per meter, default state, combined save, corrective rejection with redisplay, Select all remaining pitch-only). Place all generated files there.

---

### 10. Human review against the PRD and cross-cutting contract

**Type**: REVIEW
**Output**: A human verifies the implementation against the issue's "How to verify" and "Acceptance criteria" sections, the PRD's user stories 17, 18, and 20, and cross-cutting contract §2 (the whole parameter-form contract: hidden `workflowVersion` with compare-and-set, safe invalid-value redisplay through the one-time validation state, focused accessible error summary and field-error wiring, native constraints, hostile form shapes, and downstream invalidation), §4 (token table row for `POST /etude/notes`: `workflowVersion` CAS, increments), and §5 (the notes step is one coherent prerequisite — confirmed only when both pitches and durations are confirmed; this issue confirms the duration half and drives `notesConfirmed` via the combined save). It also confirms the corrective message exposes no catalog pattern, internal token letter, or catalog line number.
**Depends on**: 9

This is a human-in-the-loop step. The human must verify: (a) given a selected meter, only durations that appear in at least one catalog pattern for that meter are offered; (b) given a newly derived notes step, every individually compatible duration is selected; (c) given a submitted duration set with at least one eligible complete-measure pattern, it is accepted and persisted in canonical order; (d) given a submitted duration set with no eligible pattern, it is rejected with the stable corrective message naming the duration group and at least one concrete duration to add, nothing is persisted, and the submitted selection is redisplayed; (e) an empty duration selection is rejected; (f) duplicate submissions of the same duration are de-duplicated and accepted; (g) an unknown duration token, or one not offerable for the current meter, is rejected with a field-addressable error rather than silently dropped; (h) the corrective message exposes no catalog pattern, internal token letter, or catalog line number; (i) a stale workflow version is rejected and the currently saved durations are shown with an explanatory error; (j) given confirmed durations and confirmed pitches, the notes step is complete and the split or review step becomes reachable; given only one of the two, the notes step remains the earliest incomplete step; (k) the pitch half (Issue 13) still works — Select all is pitch-only and a combined save is required to confirm the step. Record the result in the review notes. Do not modify the parent issue or the parent PRD.

---
