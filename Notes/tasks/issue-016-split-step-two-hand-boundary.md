# Tasks for #16: Conditional split step for the two-hand boundary

Parent issue: #16
Parent PRD: `Notes/PRD-etude-generator.md`

## Tasks

### 1. Write failing Bun tests for `deriveEligibleBoundaries` in the music domain

**Type**: RED
**Output**: New cases in `tests/music-domain.spec.ts` that fail because `deriveEligibleBoundaries` does not exist yet. The tests assert the eligible boundary list derived from the currently selected pitches: a boundary is offered between each adjacent pair of selected pitches (in available-set order), lower pitches go left and higher pitches go right, both sets are non-empty, and the empty/single-pitch selections yield an empty boundary list.
**Depends on**: none

Before writing any production code, read and follow the coding standards in `Notes/skills/AGENTS.md` and the project `AGENTS.md` (red-green-refactor, `bun:test` with `describe`/`it`/`expect`, arrow functions, explicit types, no `any`, kebab-case filenames, braces around all `if`/`while` bodies). Read `Notes/skills/code-writing/typescript-rules` for the TypeScript conventions. Look at the existing `tests/music-domain.spec.ts` for the closest `bun:test` style and at `src/lib/music-domain.ts` for the `deriveAvailablePitches`/`deriveScaleRangePitches` helpers and the Mozilla header/`@module` doc convention.

Add a `describe` block for `deriveEligibleBoundaries`, importing it from `src/lib/music-domain.ts`. Cover: (a) given two selected pitches in available-set order (e.g. `['C4','D4']`), returns exactly one boundary between them, with the lower pitch assigned to the left hand and the higher to the right, both sets non-empty; (b) given three selected pitches (e.g. `['C4','E4','G4']`), returns two boundaries — one between C4 and E4 (left `[C4]`, right `[E4,G4]`) and one between E4 and G4 (left `[C4,E4]`, right `[G4]`); (c) given the full C-major octave-4 set (eight pitches), returns seven boundaries, each splitting the set into a non-empty lower and non-empty upper partition; (d) given a single selected pitch, returns an empty boundary list (no boundary can leave both hands non-empty); (e) given an empty selected-pitches array, returns an empty boundary list; (f) the function is pure — it does not mutate its argument, throws, or touch the DB; (g) the boundary identifiers are stable strings derived from the adjacent pitch pair (so the form can use them as radio values and the validator can match a submitted boundary back to a pair), and the returned boundaries are ordered by the position of the split in the selected-pitch array. These tests must fail because `deriveEligibleBoundaries` does not exist yet.

---

### 2. Implement `deriveEligibleBoundaries` in the music domain

**Type**: GREEN
**Output**: `deriveEligibleBoundaries` exported from `src/lib/music-domain.ts` as a pure arrow function. The task-1 tests pass, and the full `tests/music-domain.spec.ts` suite passes.
**Depends on**: 1

Before writing production code, read and follow the coding standards in `Notes/skills/AGENTS.md` and the project `AGENTS.md` (arrow functions, explicit types, no `any`, braces around all `if`/`while` bodies, Mozilla header comment, `@module` doc). Read `Notes/skills/code-writing/typescript-rules`. Look at the existing `deriveAvailablePitches` and `deriveScaleRangePitches` in `src/lib/music-domain.ts` for the pure-function style and the `AvailablePitches` interface pattern.

Add `deriveEligibleBoundaries` to `src/lib/music-domain.ts`. Export a typed `EligibleBoundary` interface (a stable `id` string, the lower-pitch `left` array, the higher-pitch `right` array, and the two adjacent pitch names that form the split) and `deriveEligibleBoundaries(selectedPitches: readonly string[]): EligibleBoundary[]`. The implementation: (1) when `selectedPitches` has fewer than two elements, return `[]`; (2) otherwise iterate over adjacent pairs in the given order, and for each pair at index `i` build a boundary whose `left` is `selectedPitches.slice(0, i + 1)`, whose `right` is `selectedPitches.slice(i + 1)`, and whose `id` is a deterministic string built from the two adjacent pitch names (e.g. `${lower}|${upper}`) so the form and validator share a single stable identifier; (3) both partitions are non-empty by construction; (4) the function is pure — it never mutates its argument or throws. The caller is responsible for passing the selected pitches in available-set order (the notes step already stores them that way). Run the task-1 tests and the full `tests/music-domain.spec.ts` suite to confirm they pass.

---

### 3. Write failing Bun tests for the split-boundary validator

**Type**: RED
**Output**: A failing `tests/split-boundary-validator.spec.ts` that asserts `validateSplitBoundary` (reject a boundary not between adjacent selected pitches, reject one that would leave a hand empty, reject an unknown/absent/tampered value, accept an eligible boundary and return the left/right assignment) and `resolveSplitBoundaryState` (no stored boundary → none preselected; a stored boundary still eligible → preselected; a stored boundary no longer eligible → discarded and none preselected, never an ineligible option).
**Depends on**: 2

Before writing any production code, read and follow the coding standards in `Notes/skills/AGENTS.md` and the project `AGENTS.md` (red-green-refactor, `bun:test`, arrow functions, explicit types, no `any`, kebab-case filenames, braces around all `if`/`while` bodies). Read `Notes/skills/code-writing/typescript-rules`. Look at `tests/pitch-selection-validator.spec.ts` for the closest existing `bun:test` style (the `unwrap`/`unwrapErr` helpers, the `Result` import from `true-myth/result`, the header comment block) and at `src/lib/pitch-selection-validator.ts` for the `normalizeSubmitted`/`validateXSelection`/`resolveXState` pure-module pattern.

Create `tests/split-boundary-validator.spec.ts` importing from `bun:test`, `true-myth/result`, and the not-yet-existing `src/lib/split-boundary-validator.ts`, plus `deriveEligibleBoundaries` from `src/lib/music-domain.ts`. Cover `validateSplitBoundary(submitted: unknown, eligibleBoundaries: EligibleBoundary[]): Result<EligibleBoundary, SplitBoundaryFailure[]>`: (a) a non-string submitted value (null, undefined, a number, an array, an object) → `Result.err` with a field-addressable failure on `'boundary'`; (b) an empty or all-whitespace string → `Result.err` with the empty-selection message; (c) a submitted `id` that matches an eligible boundary → `Result.ok` with that boundary (and thus the left/right assignment); (d) a submitted `id` that is not among the eligible boundaries (e.g. a stale boundary from a prior pitch selection, or a tampered value) → `Result.err` with a field-addressable failure, nothing accepted; (e) when the eligible boundary list is empty (fewer than two selected pitches) any submission → `Result.err` with a field-addressable failure; (f) the function is pure — it never throws or mutates. Cover `resolveSplitBoundaryState(storedBoundary: string | null, eligibleBoundaries: EligibleBoundary[]): { selectedBoundaryId: string | null; isFirstDerivation: boolean }`: (g) when `storedBoundary` is null or empty → `{ selectedBoundaryId: null, isFirstDerivation: true }` (no boundary is preselected on a first derivation — the student must choose); (h) when `storedBoundary` matches an eligible boundary → `{ selectedBoundaryId: <that id>, isFirstDerivation: false }`; (i) when `storedBoundary` no longer matches any eligible boundary (stale after an upstream change) → `{ selectedBoundaryId: null, isFirstDerivation: false }` — the stored value is discarded and no ineligible option is preselected. These tests must fail because `src/lib/split-boundary-validator.ts` does not exist yet.

---

### 4. Implement the split-boundary validator

**Type**: GREEN
**Output**: `src/lib/split-boundary-validator.ts` exports the `SplitBoundaryFailure` interface, `EMPTY_BOUNDARY_MESSAGE`, `validateSplitBoundary`, and `resolveSplitBoundaryState` as pure arrow functions. The task-3 tests pass.
**Depends on**: 3

Before writing production code, read and follow the coding standards in `Notes/skills/AGENTS.md` and the project `AGENTS.md` (arrow functions, explicit types, no `any`, braces around all `if`/`while` bodies, one export per file where practical, `Result` from `true-myth/result`, Mozilla header comment, `@module` doc, typed failure interfaces, field-addressable failure shape). Read `Notes/skills/code-writing/typescript-rules`. Look at `src/lib/pitch-selection-validator.ts` for the pure-module style (the `normalizeSubmitted` helper, the deterministic accept/reject, the `resolveXState` first-derivation semantics) and at `src/lib/music-domain.ts` for the `EligibleBoundary` interface from task 2.

Create `src/lib/split-boundary-validator.ts`. Export:
- `SplitBoundaryFailure` (`{ field: 'boundary'; reason: string }`).
- `EMPTY_BOUNDARY_MESSAGE` — the exact stable string for an empty/absent boundary, e.g. `"Select a boundary between two adjacent pitches."`.
- `INELIGIBLE_BOUNDARY_MESSAGE` — the exact stable string for a submitted boundary that is not among the eligible boundaries, e.g. `"That boundary is not between two adjacent selected pitches."`.
- `validateSplitBoundary(submitted: unknown, eligibleBoundaries: readonly EligibleBoundary[]): Result<EligibleBoundary, SplitBoundaryFailure[]>` — (1) when `eligibleBoundaries` is empty, any submission yields `Result.err` with a single field-addressable failure on `'boundary'` (the student must return to the notes step); (2) when `submitted` is not a string or is an empty/whitespace string, `Result.err` with `EMPTY_BOUNDARY_MESSAGE`; (3) trim the submitted value and find the eligible boundary whose `id` matches; (4) on a match, `Result.ok` with that boundary (carrying the left/right assignment); (5) on no match, `Result.err` with `INELIGIBLE_BOUNDARY_MESSAGE`; (6) pure — never throws, never mutates.
- `resolveSplitBoundaryState(storedBoundary: string | null, eligibleBoundaries: readonly EligibleBoundary[]): { selectedBoundaryId: string | null; isFirstDerivation: boolean }` — when `storedBoundary` is null or empty/whitespace, returns `{ selectedBoundaryId: null, isFirstDerivation: true }`; otherwise trims the stored value and, if it matches an eligible boundary's `id`, returns `{ selectedBoundaryId: <id>, isFirstDerivation: false }`; if it matches no eligible boundary, returns `{ selectedBoundaryId: null, isFirstDerivation: false }` — the stale stored value is discarded and no ineligible option is ever preselected. Run the task-3 tests to confirm they pass.

---

### 5. Write failing Bun tests for `updateEtudeSplit` and `clearEtudeSplit` repository functions

**Type**: RED
**Output**: New cases in `tests/etude-params-repository.spec.ts` that fail because `updateEtudeSplit` and `clearEtudeSplit` do not exist yet. The tests assert the CAS persistence of `splitBoundary` and `splitConfirmed: true`, the workflow-version increment, the stale-version and epoch-mismatch rejections, the identical-resubmit short-circuit, the db-error handling, that downstream state is untouched, and that `clearEtudeSplit` nulls `splitBoundary`/`splitConfirmed` (and optionally `notesConfirmed` for the corrupt-state recovery) without incrementing the version beyond the documented behavior.
**Depends on**: 4

Before writing any production code, read and follow the coding standards in `Notes/skills/AGENTS.md` and the project `AGENTS.md` (red-green-refactor, `bun:test`, arrow functions, explicit types, no `any`, kebab-case filenames). Look at `tests/etude-params-repository.spec.ts` for the existing repository test pattern (`createTestDb`, `insertUser`, `unwrap`, `loadOrCreateEtudeParams`, `loadEtudeParams`, the `validSetup` constant, the `confirmSetup` helper, and the existing `updateEtudeNotes` `describe` block from Issue 14). Read `Notes/skills/code-writing/typescript-rules` and `Notes/skills/code-writing/database-access`.

Add a `describe` block for `updateEtudeSplit`, importing it from `src/lib/etude-params-repository.ts`: (a) a save with valid `expectedWorkflowVersion` and `expectedEpoch`, `splitBoundary: 'E4|G4'` → `Result.ok` with `splitBoundary` stored, `splitConfirmed: true`, `workflowVersion` incremented by 1, and `setupConfirmed`/`notesConfirmed`/`selectedPitches`/`selectedDurations` unchanged; (b) a stale `expectedWorkflowVersion` (stored - 1) → `Result.err` with `kind: 'version-mismatch'`, and a reload confirms `splitBoundary` and `splitConfirmed` unchanged; (c) a stale `expectedEpoch` (stored - 1) → `Result.err` with `kind: 'epoch-mismatch'`, nothing changed on reload; (d) an injected failure during the write — pass a `DrizzleClient` wrapper whose `update` throws, assert `result.isErr` and `error.kind === 'db-error'`, and confirm via a separate real `createTestDb` client that nothing was persisted; (e) an identical resubmit (same `splitBoundary` as stored, same version) → `Result.ok` with no version increment and `splitConfirmed` unchanged (matching the identical-resubmit short-circuit pattern); (f) a stale-version resubmit of an identical boundary → still `Result.err` `version-mismatch`. Add a `describe` block for `clearEtudeSplit`: (g) given a row with `splitBoundary` set and `splitConfirmed: true`, calling `clearEtudeSplit(db, userId, expectedEpoch)` nulls `splitBoundary` and sets `splitConfirmed: false`, and does not increment `workflowVersion` (it is a corrective clear, not a student submission); (h) a variant `clearEtudeSplit` that also unconfirms the notes step (for the corrupt-state fewer-than-two-pitches recovery) nulls `splitBoundary`, sets `splitConfirmed: false`, and sets `notesConfirmed: false`; (i) a stale `expectedEpoch` → `Result.err` with `kind: 'epoch-mismatch'`, nothing changed. These tests must fail because neither function exists yet. Decide whether `clearEtudeSplit` takes a flag or whether two functions are clearer; follow whichever keeps the CAS pattern uniform with the existing repository functions.

---

### 6. Implement `updateEtudeSplit` and `clearEtudeSplit`

**Type**: GREEN
**Output**: `updateEtudeSplit` and `clearEtudeSplit` in `src/lib/etude-params-repository.ts`. `updateEtudeSplit` conditionally sets `splitBoundary` and `splitConfirmed: true` and increments `workflowVersion` via a compare-and-set write matching the `updateEtudeNotes` CAS pattern. `clearEtudeSplit` nulls `splitBoundary` and `splitConfirmed` (and optionally `notesConfirmed` for the corrupt-state recovery) under an epoch guard without incrementing the version. The task-5 tests pass, and the full repository suite passes.
**Depends on**: 5

Before writing production code, read and follow the coding standards in `Notes/skills/AGENTS.md` and the project `AGENTS.md` (arrow functions, explicit types, no `any`, braces around all `if`/`while` bodies, `Result` from `true-myth/result`). Read `Notes/skills/code-writing/database-access` and `Notes/skills/code-writing/typescript-rules`. Look at the existing `updateEtudeNotes`/`updateEtudeNotesActual` in `src/lib/etude-params-repository.ts` (around lines 389-483) for the exact CAS pattern to follow: load → identical-resubmit short-circuit → conditional `update(...).set(...).where(eq userId, eq aggregateEpoch, eq workflowVersion).returning()` → zero-row disambiguation.

Add `updateEtudeSplit` and `updateEtudeSplitActual` to `src/lib/etude-params-repository.ts`, following the `updateEtudeNotes` CAS pattern. The signature: `updateEtudeSplit(db, userId, expectedEpoch, expectedWorkflowVersion, splitBoundary: string): Promise<Result<EtudeParams, EtudeUpdateError>>`. The implementation: (1) load the current row; if none, return `Result.err({ kind: 'version-mismatch' })`; (2) compare the submitted `splitBoundary` against the stored `splitBoundary` — if identical and the version matches, return `Result.ok` with no write (identical-resubmit short-circuit); if identical but the version is stale, return `Result.err({ kind: 'version-mismatch' })`; (3) otherwise issue a conditional `update(...).set({ splitBoundary, splitConfirmed: true, workflowVersion: sql\`${etudeParams.workflowVersion} + 1\`, updatedAt: new Date() }).where(eq userId, eq aggregateEpoch, eq workflowVersion).returning()`; (4) on zero rows, re-load and disambiguate `epoch-mismatch` vs `version-mismatch` exactly as `updateEtudeNotesActual` does; (5) on a thrown error, return `Result.err({ kind: 'db-error', error })`. Do NOT modify `selectedPitches`, `selectedDurations`, `setupConfirmed`, or `notesConfirmed`. Add `clearEtudeSplit(db, userId, expectedEpoch, unconfirmNotes: boolean): Promise<Result<EtudeParams, EtudeUpdateError>>` for the one-hand redirect and the corrupt-state recovery: it issues a conditional `update(...).set({ splitBoundary: null, splitConfirmed: false, ...(unconfirmNotes ? { notesConfirmed: false } : {}), updatedAt: new Date() }).where(eq userId, eq aggregateEpoch).returning()` — it does NOT increment `workflowVersion` (it is a corrective clear, not a student submission) and does NOT require a version match, only the epoch guard, so a one-hand redirect or a corrupt-state recovery can clear stale split state even when the version is unknown. On zero rows, re-load and disambiguate `epoch-mismatch` vs `version-mismatch` (treat a missing row as `version-mismatch`). Run the task-5 tests and the full repository suite to confirm they pass.

---

### 7. Write failing Bun tests for the canonical-route split/review rows and PATHS constants

**Type**: RED
**Output**: New cases in `tests/canonical-route.spec.ts` that fail because `resolveCanonicalRoute` does not yet route past the notes step, and because `PATHS.ETUDE_SPLIT` and `PATHS.ETUDE_REVIEW` do not exist yet. The tests assert: two hands, notes confirmed, split unconfirmed → `/etude/split`; two hands, notes confirmed, split confirmed → `/etude/review`; one hand (left or right), notes confirmed → `/etude/review` (split skipped); two hands, notes confirmed, fewer than two stored pitches (corrupt state) → `/etude/notes` (the notes step is the earliest incomplete step).
**Depends on**: 6

Before writing any production code, read and follow the coding standards in `Notes/skills/AGENTS.md` and the project `AGENTS.md` (red-green-refactor, `bun:test`, arrow functions, explicit types, no `any`). Look at `tests/canonical-route.spec.ts` for the existing `baseParams` helper and the `resolveCanonicalRoute` test style. Read `Notes/skills/code-writing/typescript-rules`.

Add cases to `tests/canonical-route.spec.ts`: (a) `setupConfirmed: true, notesConfirmed: true, hand: 'both', splitConfirmed: false` → `'/etude/split'`; (b) `setupConfirmed: true, notesConfirmed: true, hand: 'both', splitConfirmed: true` → `'/etude/review'`; (c) `setupConfirmed: true, notesConfirmed: true, hand: 'left', splitConfirmed: false` → `'/etude/review'` (one hand skips split); (d) `setupConfirmed: true, notesConfirmed: true, hand: 'right', splitConfirmed: false` → `'/etude/review'`; (e) `setupConfirmed: true, notesConfirmed: true, hand: 'both', splitConfirmed: false, selectedPitches: 'C4'` (fewer than two stored pitches, corrupt state) → `'/etude/notes'` (the notes step is treated as the earliest incomplete step because the two-hand minimum is no longer met); (f) `setupConfirmed: true, notesConfirmed: false, hand: 'both'` → still `'/etude/notes'` (unchanged from Issue 14). Also assert that `PATHS.ETUDE_SPLIT` equals `'/etude/split'` and `PATHS.ETUDE_REVIEW` equals `'/etude/review'` by importing `PATHS` from `src/constants`. These tests must fail because the resolver currently falls through to `/etude/setup` after notes confirmed and the two PATHS constants do not exist.

---

### 8. Extend `resolveCanonicalRoute` and add the PATHS constants

**Type**: GREEN
**Output**: `PATHS.ETUDE_SPLIT` and `PATHS.ETUDE_REVIEW` added to `src/constants.ts`. `resolveCanonicalRoute` in `src/lib/canonical-route.ts` extended with the split and review rows of cross-cutting contract section 5: two hands + notes confirmed + split unconfirmed → `/etude/split`; one hand + notes confirmed → `/etude/review`; two hands + notes confirmed + split confirmed → `/etude/review`; two hands + notes confirmed + fewer than two stored pitches → `/etude/notes`. The task-7 tests pass, and the full `tests/canonical-route.spec.ts` suite passes.
**Depends on**: 7

Before writing production code, read and follow the coding standards in `Notes/skills/AGENTS.md` and the project `AGENTS.md` (arrow functions, explicit types, no `any`, braces around all `if`/`while` bodies, Mozilla header comment, `@module` doc). Read `Notes/skills/code-writing/typescript-rules`. Look at the existing `resolveCanonicalRoute` in `src/lib/canonical-route.ts` and the `PATHS` object in `src/constants.ts`.

Add `ETUDE_SPLIT: '/etude/split' as const` and `ETUDE_REVIEW: '/etude/review' as const` to the `PATHS` object in `src/constants.ts`, alongside the existing `ETUDE_SETUP` and `ETUDE_NOTES` entries. Extend `resolveCanonicalRoute` in `src/lib/canonical-route.ts` after the `!params.notesConfirmed` branch: (1) when `params.hand === 'both'` and the stored `selectedPitches` has fewer than two pitches (parse the comma-separated string; null/empty counts as zero), return `PATHS.ETUDE_NOTES` — the notes step is the earliest incomplete step because the two-hand minimum is no longer met (corrupt-state recovery, cross-cutting contract section 5 "stored values no longer validate"); (2) when `params.hand === 'both'` and `!params.splitConfirmed`, return `PATHS.ETUDE_SPLIT`; (3) otherwise (one hand with notes confirmed, or both hands with split confirmed), return `PATHS.ETUDE_REVIEW`. Keep the existing no-aggregate, setup-unconfirmed, and notes-unconfirmed branches unchanged. Update the module doc comment to reflect the new rows. Run the task-7 tests and the full `tests/canonical-route.spec.ts` suite to confirm they pass.

---

### 9. Write failing Playwright e2e tests for the split step

**Type**: RED
**Output**: `e2e-tests/etude/20-etude-split-step.spec.ts` that fails because the split route does not exist yet. The tests cover: the two-hand step appears in the flow with a working submission; the one-hand flow skips it and redirects a direct GET and a direct POST to the canonical route with no boundary stored; a direct visit while durations are unconfirmed redirects to the notes step with a safe message; the inherited stale-version rejection; the redisplay of an invalid boundary with a focused error summary; and the corrupt-state recovery for a two-hand aggregate holding fewer than two pitches.
**Depends on**: 8

Before writing any production code, read and follow the coding standards in `Notes/skills/AGENTS.md` and the project `AGENTS.md` (red-green-refactor, Playwright, `data-testid` kebab-case with `-action` suffix for submit buttons). Look at `e2e-tests/etude/16-etude-notes-duration-selection.spec.ts` for the existing notes-step e2e pattern (`testWithDatabase`, `signInUser`, `navigateToHome`, `TEST_USERS`, `SERVER_BASE_URL`, the setup-post helper, the notes-post helper, and the `getAggregateState` test-route helper pattern). Look at `e2e-tests/etude/10-etude-setup-invalid-redisplay.spec.ts` for the invalid-redisplay pattern and `e2e-tests/etude/11-etude-setup-error-summary.spec.ts` for the error-summary focus and field-error wiring. Look at `e2e-tests/etude/12-etude-setup-stale-version.spec.ts` for the stale-version rejection pattern. Look at `e2e-tests/support/` for available helpers and `e2e-tests/sign-in/` for sign-in examples.

Create `e2e-tests/etude/20-etude-split-step.spec.ts` importing from `@playwright/test` and the support helpers, wrapped in `testWithDatabase`. Sign in as a `KNOWN_USER`, navigate to `/etude` to create the aggregate, submit a valid setup with `hand: 'both'` and a meter/key that yields several available pitches (e.g. C major, octave 4 → C4..C5), then submit a valid notes selection (at least two pitches and a valid duration set) so `notesConfirmed` becomes true, then navigate to `/etude/split`. Cover: (a) the two-hand step appears — the split form renders one radio per eligible boundary (e.g. seven radios for the eight C-major-octave-4 pitches) with `data-testid` like `boundary-field-<id>`, a hidden `workflowVersion` field, an accessible legend, and a Save button (`data-testid='split-save-action'`); (b) a working submission — choose a boundary, POST via the form, assert a 303 redirect to `/etude/review`, and via the `getAggregateState` test-route helper confirm `splitBoundary` is stored and `splitConfirmed: true`; (c) one-hand flow skips the split step — submit a setup with `hand: 'right'`, confirm notes, then a direct `GET /etude/split` redirects to the canonical route (`/etude/review`) with a safe message and no boundary is stored; (d) a direct `POST /etude/split` for a one-hand workflow also redirects to the canonical route and clears any previously stored boundary (use the test-route helper to confirm `splitBoundary` is null afterward); (e) a direct visit while durations are unconfirmed — submit setup with `hand: 'both'`, confirm pitches but not durations, then `GET /etude/split` redirects to `/etude/notes` with a safe message; (f) the inherited stale-version rejection — POST `/etude/split` with a `workflowVersion` that is one less than the stored version and a valid boundary, assert a 303 redirect to `/etude/split` with an explanatory error and the currently saved state shown (no persistence); (g) redisplay of an invalid boundary — POST `/etude/split` with a `workflowVersion` and a `boundary` value that is not among the eligible boundaries (e.g. a stale or tampered id), assert a 303 redirect back to `/etude/split`, the field-level boundary error appears, the error summary is present and focused and links into the boundary group, and nothing was persisted; (h) corrupt-state recovery — via the test-route helper set the aggregate to `hand: 'both'`, `notesConfirmed: true`, `selectedPitches: 'C4'` (fewer than two), then `GET /etude/split` redirects to `/etude/notes` with a safe corrective message and the notes step is treated as unconfirmed (the test-route helper confirms `notesConfirmed: false` afterward). These tests must fail because `GET`/`POST /etude/split` do not exist yet.

---

### 10. Implement `GET /etude/split` and `POST /etude/split`

**Type**: GREEN
**Output**: `src/routes/build-etude-split.tsx` attaches `GET /etude/split` and `POST /etude/split` to the app, wired into the app's route registration. `GET` renders the boundary form for a two-hand workflow whose notes step is fully confirmed and that holds at least two selected pitches, with no ineligible option preselected. `POST` validates the submitted boundary, persists via `updateEtudeSplit` (CAS) setting `splitConfirmed: true`, and redirects 303 to `/etude/review` on success. One-hand direct GET/POST redirect to the canonical route and clear any stored boundary. A two-hand workflow with unconfirmed notes redirects to `/etude/notes`. A two-hand workflow with fewer than two stored pitches clears the notes/split state and redirects to `/etude/notes` with a safe corrective message. The task-9 e2e tests pass.
**Depends on**: 8, 9

Before writing production code, read and follow the coding standards in `Notes/skills/AGENTS.md` and the project `AGENTS.md` (arrow functions, explicit types, no `any`, braces around all `if`/`while` bodies, `Result` from `true-myth/result`, `data-testid` kebab-case with `-action` suffix, `value` attribute for form inputs, `redirectWithMessage`/`redirectWithError` from `src/lib/redirects.tsx` — never `c.redirect`). Read `Notes/skills/code-writing/typescript-rules`, `Notes/skills/code-writing/web-behavior`, and `Notes/skills/code-writing/styling-html-and-tsx`. Look at `src/routes/build-etude-notes.tsx` for the closest existing route pattern: the `signedInAccess` middleware, `secureHeaders(ALLOW_SCRIPTS_SECURE_HEADERS)` for GET (because of the error-summary focus script) and `secureHeaders(STANDARD_SECURE_HEADERS)` for POST, the `loadEtudeParams`/`resolveCanonicalRoute`/`parseWorkflowVersionField`/`shapeRedisplayPayload`/`redirectWithValidationState`/`consumeValidationStateFromRequest` flow, the `ErrorSummary`/`buildErrorSummaryEntries`/`buildErrorSummaryFocusScript` wiring, and the `handleUnexpectedError`/`logError`/`sanitizeError` error handling. Look at how `build-etude-notes.tsx` is registered in the app entry so `build-etude-split.tsx` is registered the same way.

Create `src/routes/build-etude-split.tsx`. The `GET` handler: (1) require `user`/`db`, else redirect to sign-in; (2) `loadEtudeParams`; on error, unexpected-error path; null → redirect to `/etude`; (3) if `!params.setupConfirmed` or `!params.notesConfirmed`, redirect to `resolveCanonicalRoute(params)` with a safe message (this covers the "durations unconfirmed" case — the canonical route is `/etude/notes`); (4) if `params.hand !== 'both'`, this is a one-hand direct visit: call `clearEtudeSplit(db, user.id, params.aggregateEpoch, false)` to clear any stored boundary, then redirect to `resolveCanonicalRoute(params)` (which will be `/etude/review`) with a safe message — no split value is stored; (5) if `params.hand === 'both'`, parse the stored `selectedPitches` into an array; if fewer than two pitches are stored (corrupt state), call `clearEtudeSplit(db, user.id, params.aggregateEpoch, true)` to null the split boundary, unconfirm the notes step, and then redirect to `/etude/notes` with a safe corrective message — the split step never renders an empty or single-option boundary list and never invents a boundary; (6) otherwise derive `eligibleBoundaries = deriveEligibleBoundaries(selectedPitchesArray)`, resolve the preselected boundary via `resolveSplitBoundaryState(params.splitBoundary, eligibleBoundaries)` (a stale stored boundary is discarded and none is preselected), consume any pending validation-state record for redisplay, and render the form. The form: a hidden `workflowVersion` field, a `<fieldset>` with an accessible legend, one radio per eligible boundary (`name='boundary'`, `value=<id>`, `id`/`data-testid` `boundary-field-<id>`, an accessible `<label>` describing the split — e.g. "Left: C4, E4 | Right: G4, A4, B4, C5" — using the boundary's `left`/`right` arrays, `aria-describedby` wiring into the boundary instructions and boundary errors, native HTML `required` constraint), the error summary (focused on redisplay via `buildErrorSummaryFocusScript`), and a Save button (`data-testid='split-save-action'`). Use the `value` attribute (not `defaultValue`) for the radio checked state. The `POST` handler: (1) require `user`/`db`; (2) parse the form tolerating hostile shapes; (3) parse the hidden `workflowVersion` field — a missing/tampered/stale value is a safe stale-form rejection redirecting to `/etude/split` with an explanatory error; (4) `loadEtudeParams`; null → redirect to `/etude`; if `!setupConfirmed` or `!notesConfirmed` → redirect to `resolveCanonicalRoute(params)`; (5) if `params.hand !== 'both'`, call `clearEtudeSplit(db, user.id, params.aggregateEpoch, false)` and redirect to `resolveCanonicalRoute(params)` with a safe message — no boundary is stored; (6) if `params.hand === 'both'` and fewer than two stored pitches, call `clearEtudeSplit(db, user.id, params.aggregateEpoch, true)` and redirect to `/etude/notes` with a safe corrective message; (7) otherwise derive `eligibleBoundaries`, validate the submitted `boundary` via `validateSplitBoundary(form.get('boundary'), eligibleBoundaries)`; on failure, shape a redisplay payload via `shapeRedisplayPayload({ boundary: <submitted string> }, fieldErrors)` and `redirectWithValidationState` to `/etude/split` — nothing is persisted; (8) on success call `updateEtudeSplit(db, user.id, params.aggregateEpoch, versionParseResult.value, validatedBoundary.id)`; handle conflicts exactly as the notes handler does (`version-mismatch`/`epoch-mismatch` → redirect with an explanatory error showing the newly current saved state; `db-error` → unexpected-error path); (9) on success redirect 303 to `/etude/review` via `redirectWithMessage`. Register `buildEtudeSplit` in the app entry alongside `buildEtudeNotes`. Run the task-9 e2e tests to confirm they pass, along with the existing notes/setup e2e tests to confirm no regression.

---

### 11. Refactor and full test run

**Type**: REFACTOR
**Output**: No duplicated boundary logic between `src/lib/`, `src/routes/`, and `tests/`; the eligible-boundary derivation lives only in `src/lib/music-domain.ts`, the boundary validation/resolution only in `src/lib/split-boundary-validator.ts`, and the CAS/clear persistence only in `src/lib/etude-params-repository.ts`; the split route does not inline any of that logic; `tsc --noEmit` reports zero errors in any file created or modified for this issue; the full `bun test` suite and the `npx playwright test` suite pass.
**Depends on**: 10

Read and follow the coding standards in `Notes/skills/AGENTS.md` and the project `AGENTS.md`. Search `src/routes/`, `src/lib/`, `src/components/`, and `tests/` for any duplicated boundary logic, inline eligible-boundary computation that should call `deriveEligibleBoundaries`, inline boundary validation that should call `validateSplitBoundary`, inline stale-boundary discard that should call `resolveSplitBoundaryState`, or inline CAS/clear logic that should call `updateEtudeSplit`/`clearEtudeSplit`. Move any such logic behind the appropriate `src/lib/` module. Confirm the `updateEtudeSplit`/`clearEtudeSplit` CAS pattern is not duplicated inline in the route. Run `tsc --noEmit`, the full `bun test` suite, and `npx playwright test` and confirm they are green. Do not modify the parent issue, the parent PRD, or prior task/issue/walkthrough files in `Notes/`.

---

### 12. Update wiki and notes documentation

**Type**: DOCUMENT
**Output**: Wiki and Notes updates describing the split step: the `deriveEligibleBoundaries` pure function in `src/lib/music-domain.ts` (boundaries between adjacent selected pitches, both hands non-empty), the `validateSplitBoundary`/`resolveSplitBoundaryState` pure functions in `src/lib/split-boundary-validator.ts` (ineligible/empty-hand rejection, stale-boundary discard), the `updateEtudeSplit`/`clearEtudeSplit` repository functions (CAS persistence of `splitBoundary`/`splitConfirmed`, the corrective clear for one-hand redirects and corrupt-state recovery), the extended `resolveCanonicalRoute` split/review rows and the new `PATHS.ETUDE_SPLIT`/`PATHS.ETUDE_REVIEW` constants, and the new `GET`/`POST /etude/split` route in `src/routes/build-etude-split.tsx` (two-hand rendering, one-hand redirect-and-clear, notes-unconfirmed redirect, corrupt-state recovery, stale-version rejection, invalid-boundary redisplay). Follow `Notes/wiki/wiki-rules.md` and `Notes/wiki/AGENTS.md` for the wiki ingest operation.
**Depends on**: 11

Before writing documentation, read `Notes/wiki/AGENTS.md` and `Notes/wiki/wiki-rules.md` for the wiki conventions (ingest operation, kebab-case filenames, update `index.md` and append to `log.md` with the `## [YYYY-MM-DD] <operation> | <subject>` format).

Update the relevant wiki pages: `Notes/wiki/source-code.md` (add `src/lib/split-boundary-validator.ts`, the `deriveEligibleBoundaries` function in `src/lib/music-domain.ts`, the `updateEtudeSplit`/`clearEtudeSplit` functions in `src/lib/etude-params-repository.ts`, the extended `resolveCanonicalRoute` in `src/lib/canonical-route.ts`, the new `PATHS.ETUDE_SPLIT`/`PATHS.ETUDE_REVIEW` constants, and the new route handlers in `src/routes/build-etude-split.tsx`), `Notes/wiki/e2e-tests.md` (catalog `e2e-tests/etude/20-etude-split-step.spec.ts`), `Notes/wiki/unit-tests.md` (catalog the new `tests/split-boundary-validator.spec.ts`, the new `music-domain.spec.ts` cases, the new `etude-params-repository.spec.ts` cases, and the new `canonical-route.spec.ts` cases), `Notes/wiki/project-overview.md` (describe the split step: boundary between adjacent selected pitches, lower-left/higher-right assignment, both hands non-empty, one-hand skip with redirect-and-clear, notes-unconfirmed redirect, corrupt-state recovery, stale-boundary discard, and the 303 to `/etude/review` on success), and `Notes/wiki/index.md` if new sections are added. Append a `## [YYYY-MM-DD] ingest | issue-016 split step two-hand boundary` entry to `Notes/wiki/log.md`. Do not modify the parent issue or the parent PRD.

---

### 13. Code walkthrough

**Type**: CODE WALKTHROUGH
**Output**: `Notes/walkthroughs/issue-016-split-step-two-hand-boundary/code-walkthrough/` containing the files generated by `uvx showboat` for this implementation.
**Depends on**: 12

Run `uvx showboat --help` for current usage, then generate a walkthrough of the issue-016 implementation into a new directory `Notes/walkthroughs/issue-016-split-step-two-hand-boundary/code-walkthrough/`. The walkthrough should cover: (1) the `deriveEligibleBoundaries` pure function in `src/lib/music-domain.ts` (boundaries between adjacent selected pitches, both hands non-empty, stable ids), (2) the `validateSplitBoundary` and `resolveSplitBoundaryState` pure functions in `src/lib/split-boundary-validator.ts` (ineligible/empty-hand/unknown rejection, stale-boundary discard with no ineligible preselect), (3) the `updateEtudeSplit` and `clearEtudeSplit` repository functions in `src/lib/etude-params-repository.ts` (CAS persistence, `splitConfirmed: true`, version increment, identical-resubmit short-circuit, stale-version/epoch-mismatch rejections, the corrective clear for one-hand and corrupt-state recovery), (4) the extended `resolveCanonicalRoute` split/review rows and the new `PATHS` constants, and (5) the `GET`/`POST /etude/split` handlers in `src/routes/build-etude-split.tsx` (two-hand rendering, one-hand redirect-and-clear, notes-unconfirmed redirect, corrupt-state recovery, stale-version rejection, invalid-boundary redisplay with a focused error summary, and the 303 to `/etude/review` on success). Place all generated files there.

---

### 14. Human review against the PRD and cross-cutting contract

**Type**: REVIEW
**Output**: A human verifies the implementation against the issue's "How to verify" and "Acceptance criteria" sections, the PRD's user stories 21 and 22, and cross-cutting contract §2 (the whole parameter-form contract: hidden `workflowVersion` with compare-and-set, safe invalid-value redisplay through the one-time validation state, focused accessible error summary and field-error wiring, native constraints, hostile form shapes, and downstream invalidation), §4 (token table row for `POST /etude/split`: `workflowVersion` CAS, increments), and §5 (the split step's prerequisite is the complete notes step — pitches and durations both confirmed; split is never reachable, and never chosen as the earliest incomplete step, while either half is unconfirmed; one-hand workflows skip split; the corrupt-state fewer-than-two-pitches row returns the student to the notes step).
**Depends on**: 13

This is a human-in-the-loop step. The human must verify: (a) given both hands and at least two selected pitches, the split step offers a boundary between each adjacent pair and no others; (b) given a chosen boundary, lower pitches are assigned left, higher pitches right, both sets are non-empty, the assignment is persisted, and the response is a 303 redirect to `GET /etude/review`; (c) given a submitted boundary that would leave a hand empty or is not between adjacent selected pitches, it is rejected, nothing is persisted, and the step is redisplayed with a focused error summary; (d) given a one-hand workflow, the split step is absent from the workflow, a direct GET and a direct POST both redirect to the canonical route, and no boundary is stored (any previously stored boundary is cleared); (e) given a two-hand workflow whose durations are unconfirmed, a direct split request redirects to the notes step with a safe message; (f) given a two-hand workflow with fewer than two stored pitches, the split step never renders and the student is returned to the notes step with the step treated as unconfirmed; (g) given a stored boundary that is no longer eligible, it is discarded and no ineligible option is preselected; (h) given a submission carrying a stale workflow version, it is rejected and the currently saved state is shown with an explanatory error. Record the result in the review notes. Do not modify the parent issue or the parent PRD.

---
