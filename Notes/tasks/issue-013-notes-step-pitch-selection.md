# Tasks for #13: Notes step pitch selection with defaults, Select all, and cardinality rules

Parent issue: #13
Parent PRD: `Notes/PRD-etude-generator.md`

## Tasks

### 1. Add `ETUDE_NOTES` path constant

**Type**: CONFIG
**Output**: `src/constants.ts` PATHS object has a new `ETUDE_NOTES: '/etude/notes'` entry; `tsc --noEmit` passes; the existing `bun test` suite still passes.
**Depends on**: none

Read and follow the coding standards in `Notes/skills/AGENTS.md` and the project `AGENTS.md`. Look at the existing PATHS object in `src/constants.ts` (around lines 23-75) for the convention: etude paths are `ETUDE: '/etude'` and `ETUDE_SETUP: '/etude/setup'`, both typed `as const`.

Add `ETUDE_NOTES: '/etude/notes' as const` to the PATHS object in `src/constants.ts`, placed immediately after `ETUDE_SETUP`. This is the canonical route path for the notes step, referenced by the canonical-route resolver (task 7) and the notes route handlers (task 9). Run `tsc --noEmit` to confirm it compiles.

---

### 2. Write failing Bun tests for pitch-selection module

**Type**: RED
**Output**: A failing `tests/pitch-selection-validator.spec.ts` that asserts two pure functions: `validatePitchSelection` (cardinality rules, unavailable-pitch rejection, hostile-shape tolerance) and `resolvePitchSelectionState` (first-derivation all-selected default, non-re-expansion of a stored narrowed selection, re-derivation after an Issue 11 clear).
**Depends on**: 1

Before writing any production code, read and follow the coding standards in `Notes/skills/AGENTS.md` and the project `AGENTS.md` (red-green-refactor, `bun:test` with `describe`/`it`/`expect`, arrow functions, explicit types, no `any`, kebab-case filenames, braces around all `if`/`while` bodies). Read `Notes/skills/code-writing/typescript-rules` for the TypeScript conventions. Look at `tests/music-domain.spec.ts` and `tests/etude-invalidation.spec.ts` for the existing `bun:test` style in this repo (the `unwrap`/`unwrapErr` helpers, the `Result` import from `true-myth/result`, the header comment block).

Create `tests/pitch-selection-validator.spec.ts` importing from `bun:test` and from the not-yet-existing `src/lib/pitch-selection-validator.ts`. Cover `validatePitchSelection(submitted: unknown, available: string[], hand: string): Result<string[], PitchSelectionFailure[]>`: (a) one-hand mode (`hand: 'left'` or `'right'`), zero pitches → `Result.err` with a field-addressable failure on `'pitches'`; (b) one-hand mode, one pitch → `Result.ok`; (c) one-hand mode, multiple pitches → `Result.ok`; (d) two-hand mode (`hand: 'both'`), zero pitches → `Result.err` with the exact message `"Select at least two pitches when using both hands."`; (e) two-hand mode, one pitch → `Result.err` with that same exact message; (f) two-hand mode, two pitches → `Result.ok`; (g) a submitted pitch not in the available set → `Result.err` with a field-addressable failure naming the unavailable pitch; (h) a submitted pitch that is available → accepted; (i) duplicate submitted pitches → deduplicated to a single entry and accepted (or rejected per the validator's design, but deterministically, never a 500); (j) reordered submitted pitches → accepted, order normalized to the available-set order; (k) a non-array `submitted` (null, undefined, a string, a number) → `Result.err` with a field-addressable failure, never a thrown exception; (l) an empty available set with any submission → `Result.err` (no pitch can be available); (m) the function is pure — it does not mutate its arguments, touch the DB, or throw for any of the above inputs. Cover `resolvePitchSelectionState(storedPitches: string | null, available: string[]): { selectedPitches: string[]; isFirstDerivation: boolean }`: (n) `storedPitches: null` → `{ selectedPitches: available, isFirstDerivation: true }` (first derivation preselects all available pitches); (o) `storedPitches: 'C4,D4'` with `available: ['C4','D4','E4']` → `{ selectedPitches: ['C4','D4'], isFirstDerivation: false }` (stored narrowed selection is not re-expanded); (p) `storedPitches: 'C4'` with `available: ['C4','D4','E4']` → `{ selectedPitches: ['C4'], isFirstDerivation: false }` (a single-pitch narrowed selection is shown as-is); (q) `storedPitches: ''` (empty string) → treated as null → first derivation with all available preselected; (r) `storedPitches: 'C4,D4'` with `available: ['C4']` (stored pitch D4 no longer available) → `{ selectedPitches: ['C4'], isFirstDerivation: false }` (unavailable stored pitches are filtered out, available ones retained); (s) the function is pure — it does not mutate its arguments or throw. These tests must fail because the module does not exist yet.

---

### 3. Implement pitch-selection module

**Type**: GREEN
**Output**: `src/lib/pitch-selection-validator.ts` exports the `PitchSelectionFailure` interface, `validatePitchSelection`, and `resolvePitchSelectionState` as pure arrow functions. The task-2 tests pass.
**Depends on**: 2

Before writing production code, read and follow the coding standards in `Notes/skills/AGENTS.md` and the project `AGENTS.md` (arrow functions, explicit types, no `any`, braces around all `if`/`while` bodies, one export per file where practical, `Result` from `true-myth/result`). Read `Notes/skills/code-writing/typescript-rules` for the TypeScript conventions. Look at `src/lib/music-domain.ts` and `src/lib/setup-validator.ts` for the existing pure-domain-module style (the Mozilla header comment, the `@module` doc, the `Result` from `true-myth/result`, the typed failure interfaces, the field-addressable failure shape).

Create `src/lib/pitch-selection-validator.ts`. Export `PitchSelectionFailure` (`{ field: 'pitches'; reason: string }`) and `validatePitchSelection(submitted: unknown, available: string[], hand: string): Result<string[], PitchSelectionFailure[]>`. The function: (1) normalizes `submitted` to a `string[]` — a non-array (null, undefined, non-array) yields an empty array (zero pitches), an array of strings is mapped and trimmed, non-string elements are stringified then trimmed; (2) deduplicates and filters the submitted pitches to only those in the `available` set — any submitted pitch not in `available` is a field-addressable failure with a safe message naming that the pitch is not available; (3) enforces cardinality after the availability filter: when `hand === 'both'` and fewer than two pitches remain, emit a failure with the exact message `"Select at least two pitches when using both hands."`; when `hand` is `'left'` or `'right'` and zero pitches remain, emit a failure with a safe one-hand minimum message; (4) on success, returns `Result.ok` with the available pitches in available-set order (not submission order); (5) never throws, never mutates arguments. Export `resolvePitchSelectionState(storedPitches: string | null, available: string[]): { selectedPitches: string[]; isFirstDerivation: boolean }`. The function: when `storedPitches` is null or an empty/whitespace string, returns `{ selectedPitches: [...available], isFirstDerivation: true }`; otherwise parses the comma-separated stored string, filters to only pitches in `available`, and returns `{ selectedPitches: filtered, isFirstDerivation: false }`. Both functions are pure. Run the task-2 tests to confirm they pass.

---

### 4. Write failing Bun tests for `updateEtudePitches` repository function

**Type**: RED
**Output**: New cases in `tests/etude-params-repository.spec.ts` that fail because `updateEtudePitches` does not exist yet. The tests assert the CAS persistence of `selectedPitches`, the workflow-version increment, the stale-version and epoch-mismatch rejections, the db-error handling, and that `notesConfirmed` stays false (durations are Issue 14).
**Depends on**: 3

Before writing any production code, read and follow the coding standards in `Notes/skills/AGENTS.md` and the project `AGENTS.md` (red-green-refactor, `bun:test`, arrow functions, explicit types, no `any`, kebab-case filenames). Look at `tests/etude-params-repository.spec.ts` for the existing repository test pattern (`createTestDb`, `insertUser`, `unwrap`, `loadOrCreateEtudeParams`, `loadEtudeParams`, the `validSetup` constant, the `confirmNotesAndSplit` helper). Read `Notes/skills/code-writing/typescript-rules` and `Notes/skills/code-writing/database-access`.

Add a `confirmSetup` helper to `tests/etude-params-repository.spec.ts` (if one does not already exist) that calls `updateEtudeSetup` with `validSetup` and the current epoch/version so `setupConfirmed` becomes true — the notes step is only reachable after setup is confirmed. Add `describe` blocks for `updateEtudePitches`: (a) a successful pitch save with a valid `expectedWorkflowVersion` and `expectedEpoch` → `Result.ok`, `selectedPitches` is the submitted comma-joined string, `workflowVersion` incremented by 1, `setupConfirmed` still true, `notesConfirmed` still false (durations are Issue 14), `selectedDurations` and `splitBoundary` unchanged; (b) a stale `expectedWorkflowVersion` (stored - 1) → `Result.err` with `kind: 'version-mismatch'`, and a reload confirms `selectedPitches` is unchanged and `workflowVersion` is unchanged; (c) a stale `expectedEpoch` (stored - 1) → `Result.err` with `kind: 'epoch-mismatch'`, and a reload confirms nothing changed; (d) an injected failure during the write — pass a `DrizzleClient` wrapper whose `update` throws, call `updateEtudePitches`, assert `result.isErr` and `result.error.kind === 'db-error'`, and confirm via a separate real `createTestDb` client that nothing was persisted; (e) an identical resubmit (same `selectedPitches` as stored, same version) → `Result.ok` with no version increment (the identical-resubmit short-circuit, matching `updateEtudeSetup`'s pattern); (f) saving a different pitch set after a prior save → `selectedPitches` updated, `workflowVersion` incremented again. These tests must fail because `updateEtudePitches` does not exist yet.

---

### 5. Implement `updateEtudePitches`

**Type**: GREEN
**Output**: `updateEtudePitches` in `src/lib/etude-params-repository.ts` conditionally sets `selectedPitches` and increments `workflowVersion` via a compare-and-set write matching the `updateEtudeSetup` CAS pattern. The task-4 tests pass, and the full repository suite passes.
**Depends on**: 4

Before writing production code, read and follow the coding standards in `Notes/skills/AGENTS.md` and the project `AGENTS.md` (arrow functions, explicit types, no `any`, braces around all `if`/`while` bodies, `Result` from `true-myth/result`). Read `Notes/skills/code-writing/database-access` and `Notes/skills/code-writing/typescript-rules`. Look at the existing `updateEtudeSetup` / `updateEtudeSetupActual` (around lines 252-355) for the CAS pattern: load → identical-resubmit short-circuit → conditional `update(...).set(...).where(eq userId, eq aggregateEpoch, eq workflowVersion).returning()` → zero-row disambiguation.

Add `updateEtudePitches` and `updateEtudePitchesActual` to `src/lib/etude-params-repository.ts`, following the same CAS pattern as `updateEtudeSetup`. The function signature: `updateEtudePitches(db: DrizzleClient, userId: string, expectedEpoch: number, expectedWorkflowVersion: number, selectedPitches: string[]): Promise<Result<EtudeParams, EtudeUpdateError>>`. The implementation: (1) load the current row; if none, return `Result.err({ kind: 'version-mismatch' })`; (2) compare the submitted `selectedPitches.join(',')` against `stored.selectedPitches` — if identical and the version matches, return `Result.ok` with no write (identical-resubmit short-circuit); if identical but the version is stale, return `Result.err({ kind: 'version-mismatch' })`; (3) otherwise issue a conditional `update(...).set({ selectedPitches: selectedPitches.join(','), workflowVersion: sql\`${etudeParams.workflowVersion} + 1\`, updatedAt: new Date() }).where(eq userId, eq aggregateEpoch, eq workflowVersion).returning()`; (4) on zero rows, re-load and disambiguate `epoch-mismatch` vs `version-mismatch` exactly as `updateEtudeSetupActual` does; (5) on a thrown error, return `Result.err({ kind: 'db-error', error })`. Do NOT set `notesConfirmed: true` — the notes step is confirmed only when both pitches and durations are confirmed (durations are Issue 14). Do NOT clear or modify `selectedDurations`, `splitBoundary`, `setupConfirmed`, `notesConfirmed`, or `splitConfirmed`. Run the task-4 tests and the full repository suite to confirm they pass.

---

### 6. Write failing Bun tests for `resolveCanonicalRoute` notes step

**Type**: RED
**Output**: New cases in `tests/canonical-route.spec.ts` that fail because `resolveCanonicalRoute` does not yet return `/etude/notes` when setup is confirmed and notes are unconfirmed.
**Depends on**: 1

Before writing any production code, read and follow the coding standards in `Notes/skills/AGENTS.md` and the project `AGENTS.md` (red-green-refactor, `bun:test`, arrow functions, explicit types, no `any`). Look at `tests/canonical-route.spec.ts` for the existing `baseParams` helper that builds an `EtudeParams` with overrides, and the existing test cases for the no-aggregate and setup-not-confirmed rows.

Add test cases to `tests/canonical-route.spec.ts`: (a) `setupConfirmed: true`, `notesConfirmed: false` → `resolveCanonicalRoute` returns `/etude/notes`; (b) `setupConfirmed: true`, `notesConfirmed: false`, `selectedPitches: 'C4,D4'` (pitches saved but durations not yet confirmed) → still returns `/etude/notes` (the notes step is not complete until both halves are confirmed); (c) `setupConfirmed: true`, `notesConfirmed: true`, `hand: 'right'` → returns the next step route (or `/etude/setup` as a stub if later steps are not yet wired — match whatever the current resolver returns for the post-notes state, since split/review/score rows are later issues); (d) `setupConfirmed: false` → still returns `/etude/setup` (the existing behavior is unchanged); (e) `params: null` → still returns `/etude/setup`. These tests must fail because the resolver currently returns `/etude/setup` for all confirmed-setup states.

---

### 7. Extend `resolveCanonicalRoute` for notes step

**Type**: GREEN
**Output**: `resolveCanonicalRoute` in `src/lib/canonical-route.ts` returns `PATHS.ETUDE_NOTES` when `params.setupConfirmed && !params.notesConfirmed`. The task-6 tests pass, and the existing `tests/canonical-route.spec.ts` tests still pass.
**Depends on**: 6

Before writing production code, read and follow the coding standards in `Notes/skills/AGENTS.md` and the project `AGENTS.md` (arrow functions, explicit types, no `any`, braces around all `if`/`while` bodies). Look at the existing `resolveCanonicalRoute` in `src/lib/canonical-route.ts` (around lines 31-42) for the current stub pattern.

Extend `resolveCanonicalRoute` in `src/lib/canonical-route.ts`: after the existing `!params.setupConfirmed` check (which returns `/etude/setup`), add a check for `!params.notesConfirmed` that returns `PATHS.ETUDE_NOTES`. Import `PATHS` from `../constants` (it is already imported). The notes step is the earliest incomplete step when setup is confirmed but notes are not — this matches cross-cutting contract section 5's state table row "Setup confirmed; pitches or durations unconfirmed → /etude/notes". Later issues extend this resolver for the split/review/score rows. Run the task-6 tests and the existing canonical-route tests to confirm they pass.

---

### 8. Write failing Playwright e2e tests for notes step

**Type**: RED
**Output**: `e2e-tests/etude/15-etude-notes-pitch-selection.spec.ts` that fails because the notes route does not exist yet. The tests cover the default all-selected state, Select all without scripting, one-hand and two-hand cardinality failures with the exact message, persistence of a narrowed selection, non-re-expansion, the inherited stale-version rejection, the redisplay of a narrowed selection alongside a cardinality error, and the focused error summary linking into the pitch group.
**Depends on**: 7

Before writing any production code, read and follow the coding standards in `Notes/skills/AGENTS.md` and the project `AGENTS.md` (red-green-refactor, Playwright, `data-testid` kebab-case with `-action` suffix for submit buttons). Look at `e2e-tests/etude/14-etude-downstream-invalidation.spec.ts` for the existing etude e2e pattern (`testWithDatabase`, `signInUser`, `navigateToHome`, `TEST_USERS`, `SERVER_BASE_URL`, the `postSetupViaBrowser` helper, the `seedDownstreamState` and `getAggregateState` test-route helpers). Look at `e2e-tests/etude/11-etude-setup-error-summary.spec.ts` for the error-summary focus and field-error wiring test pattern. Look at `e2e-tests/support/` for available helpers.

Create `e2e-tests/etude/15-etude-notes-pitch-selection.spec.ts` importing from `@playwright/test` and the support helpers, wrapped in `testWithDatabase`. The tests must sign in as `KNOWN_USER`, navigate to `/etude` to create the aggregate, submit a valid setup (e.g. measures 16, meter 4/4, hands right, key C major, octaves 4) via `postSetupViaBrowser` so `setupConfirmed` becomes true, then navigate to `/etude/notes`. Cover: (a) default state — every available pitch checkbox is checked (for C major octave 4, the available pitches are the C-major scale from C4 to C5 — assert each is checked); (b) Select all without scripting — deselect several pitches, submit via the Select all button (`data-testid='notes-select-all-action'`), assert a 303 redirect back to `/etude/notes`, and assert every available pitch is checked again; (c) two-hand cardinality failure — set hands to `'both'` in setup, navigate to notes, submit only one pitch via the ordinary save button (`data-testid='notes-save-action'`), assert the field-level error message `"Select at least two pitches when using both hands."` is displayed, the error summary is present and focused, and nothing was persisted (reload and confirm the prior selection is unchanged); (d) one-hand cardinality failure — with hands `'right'`, submit zero pitches (deselect all, submit), assert a cardinality rejection with a one-hand minimum message; (e) persistence of a narrowed selection — deselect several pitches, submit via the ordinary save, reload `/etude/notes`, and assert exactly the narrowed selection is checked (not re-expanded); (f) non-re-expansion — after saving a narrowed selection, navigate away and back, and assert the narrowed selection is still shown; (g) stale-version rejection — capture the `workflowVersion` from the form, submit a valid pitch set with a stale version (version - 1), assert a 303 redirect with an explanatory error and the currently saved selection is shown; (h) redisplay of a narrowed selection alongside a cardinality error — with hands `'both'`, save a valid two-pitch selection, then submit only one pitch, and assert the one submitted pitch is redisplayed checked alongside the group-level cardinality error (the error summary is focused and links into the pitch group). These tests must fail because the `/etude/notes` route does not exist yet.

---

### 9. Implement `GET /etude/notes` and `POST /etude/notes` routes

**Type**: GREEN
**Output**: `GET /etude/notes` renders the pitch checkbox form (available pitches derived from the stored key and octave range, pre-selected per first-derivation or stored selection, hidden `workflowVersion`, accessible labels, error-summary wiring). `POST /etude/notes` handles the ordinary save (validates cardinality and availability, persists via CAS, redirects 303) and Select all (ignores checkboxes, persists all available pitches, redirects 303). The task-8 e2e tests pass.
**Depends on**: 5, 7, 8

Before writing production code, read and follow the coding standards in `Notes/skills/AGENTS.md` and the project `AGENTS.md` (arrow functions, explicit types, no `any`, braces around all `if`/`while` bodies, `Result` from `true-myth/result`, `data-testid` kebab-case with `-action` suffix for submit buttons, `value` attribute for form inputs, `redirectWithMessage`/`redirectWithError` from `src/lib/redirects.tsx` — never `c.redirect`). Read `Notes/skills/code-writing/typescript-rules`, `Notes/skills/code-writing/web-behavior`, and `Notes/skills/code-writing/styling-html-and-tsx`. Look at `src/routes/build-etude.tsx` for the full setup-route pattern: the `SETUP_FIELD_SPEC`, `SETUP_FIELD_ORDER`, `SETUP_GROUP_FIELDS`, the `RedisplayData` interface, the `entriesForField`/`describedByFor`/`renderFieldErrors` helpers, the `renderEtudeSetupForm` JSX, and the GET/POST handlers (around lines 368-572). Look at how the setup form derives available pitches via `deriveAvailablePitches` from `src/lib/music-domain.ts` and `parseStoredOctaves` from `build-etude.tsx`.

Create the notes route handlers, either in a new `src/routes/build-etude-notes.tsx` or by extending `src/routes/build-etude.tsx` — follow whichever keeps the file sizes manageable and matches the existing pattern. Mount the routes in `src/index.ts` alongside `buildEtude(app)` (around line 186). The `GET /etude/notes` handler: (1) require `signedInAccess` and `secureHeaders(ALLOW_SCRIPTS_SECURE_HEADERS)` (the form needs the error-summary focus script, matching the setup GET); (2) load the aggregate via `loadEtudeParams`; if none or `!setupConfirmed`, redirect to `/etude` so the canonical resolver handles it; (3) derive available pitches via `deriveAvailablePitches(params.keySignature, parseStoredOctaves(params.selectedOctaves))` — reuse the `parseStoredOctaves` helper from `build-etude.tsx` (extract it to a shared lib module if it is not already exportable); (4) call `resolvePitchSelectionState(params.selectedPitches, availablePitches.pitches)` to determine which pitches are checked; (5) consume any pending validation-state record via `consumeValidationStateFromRequest` for redisplay; (6) render the pitch checkbox form with: a `<fieldset>` for the pitches group, one checkbox per available pitch with `id="pitches-field-<pitch>"`, `name="pitches"`, `value="<pitch>"`, `data-testid="pitch-field-<pitch>"`, `checked` when the pitch is in the resolved selection, an accessible `<label>` for each, the hidden `workflowVersion` field with `data-testid="workflow-version-field"`, the error summary (via `ErrorSummary` with `buildErrorSummaryEntries` using `NOTES_FIELD_ORDER = ['pitches']` and `NOTES_GROUP_FIELDS = { pitches: { firstMemberId: 'pitches-field-<first-available-pitch>' } }`), field-level error elements via `renderFieldErrors`, the error-summary focus script via `buildErrorSummaryFocusScript('error-summary')`, an ordinary Save button with `name="action" value="save"` and `data-testid="notes-save-action"`, and a Select all button with `name="action" value="select-all"` and `data-testid="notes-select-all-action"`; (7) when redisplay data is present, override the checked state with the safe redisplay values for the pitches field (the submitted pitches are redisplayed checked, the rest unchecked). The `POST /etude/notes` handler: (1) require `signedInAccess` and `secureHeaders(STANDARD_SECURE_HEADERS)`; (2) parse the body to FormData (same conversion as the setup POST); (3) parse the hidden `workflowVersion` via `parseWorkflowVersionField` — on error, redirect with a stale-form message; (4) read the `action` field via `form.get('action')`; (5) load the aggregate to obtain the current epoch and derive the available pitches; (6) if `action === 'select-all'`, set `selectedPitches` to all available pitches (ignoring submitted checkboxes), call `updateEtudePitches` with the CAS tokens, and redirect 303 to `/etude/notes` — Select all can never produce a cardinality error because it selects every available pitch; (7) otherwise (ordinary save), read the submitted pitches via `form.getAll('pitches')` (an empty array when no checkboxes are checked), call `validatePitchSelection(submittedPitches, availablePitches.pitches, params.hand)` — on error, shape the redisplay payload via `shapeRedisplayPayload` (with the submitted pitches as the `pitches` field) and redirect with validation state; on success, call `updateEtudePitches` with the CAS tokens and the validated pitches; (8) handle `updateEtudePitches` conflicts: `version-mismatch` and `epoch-mismatch` redirect with an explanatory error (redisplaying the committed saved state, not the submitted values); `db-error` is handled via the unexpected-error path; (9) on success, redirect 303 to `/etude/notes` via `redirectWithMessage`. Run the task-8 e2e tests to confirm they pass.

---

### 10. Refactor and full test run

**Type**: REFACTOR
**Output**: No duplicated logic between `src/lib/`, `src/routes/`, and `tests/`; the `parseStoredOctaves` helper is shared (not duplicated between the setup and notes routes); the pitch-selection validation and resolution logic lives only in `src/lib/pitch-selection-validator.ts`; the CAS pattern is not duplicated inline in the route; `tsc --noEmit` reports zero errors in any file created or modified for this issue; the full `bun test` suite and the `npx playwright test` suite pass.
**Depends on**: 9

Read and follow the coding standards in `Notes/skills/AGENTS.md` and the project `AGENTS.md`. Search `src/routes/`, `src/lib/`, `src/components/`, and `tests/` for any duplicated pitch-selection logic, inline available-pitch derivation that should call `deriveAvailablePitches`, duplicated `parseStoredOctaves` definitions, or inline cardinality checks that should call `validatePitchSelection`. Move any such logic behind the appropriate `src/lib/` module. If `parseStoredOctaves` was duplicated between `build-etude.tsx` and the notes route, extract it to a shared location (e.g. `src/lib/music-domain.ts` or a new helper) and import from both. Run `tsc --noEmit`, the full `bun test` suite, and `npx playwright test` and confirm they are green. Do not modify the parent issue, the parent PRD, or prior task/issue/walkthrough files in `Notes/`.

---

### 11. Update wiki and notes documentation

**Type**: DOCUMENT
**Output**: Wiki and Notes updates describing the notes pitch-selection step, the `validatePitchSelection` and `resolvePitchSelectionState` pure functions, the first-derivation semantics (all-selected default, non-re-expansion, re-derivation after Issue 11 clear), the Select all control (distinct submit button, no scripting, never a cardinality error), the cardinality rules with the exact two-hand message, the `updateEtudePitches` repository function, the `resolveCanonicalRoute` notes-step extension, the new `ETUDE_NOTES` path, and the new test files. Follow `Notes/wiki/wiki-rules.md` and `Notes/wiki/AGENTS.md` for the wiki ingest operation.
**Depends on**: 10

Before writing documentation, read `Notes/wiki/AGENTS.md` and `Notes/wiki/wiki-rules.md` for the wiki conventions (ingest operation, kebab-case filenames, update `index.md` and append to `log.md` with the `## [YYYY-MM-DD] <operation> | <subject>` format).

Update the relevant wiki pages: `Notes/wiki/source-code.md` (add `src/lib/pitch-selection-validator.ts`, the `updateEtudePitches` function in `src/lib/etude-params-repository.ts`, the notes route handlers, and the `ETUDE_NOTES` path in `src/constants.ts`), `Notes/wiki/e2e-tests.md` (catalog `e2e-tests/etude/15-etude-notes-pitch-selection.spec.ts`), `Notes/wiki/unit-tests.md` (catalog the new `tests/pitch-selection-validator.spec.ts` and the new repository test cases), `Notes/wiki/project-overview.md` (describe the notes pitch-selection step: the available-pitch derivation from the stored key and octave range, the first-derivation all-selected default, the non-re-expansion of a stored narrowed selection, the Select all control without scripting, the cardinality rules with the exact two-hand message, the unavailable-pitch rejection, and the canonical-route extension to `/etude/notes`), and `Notes/wiki/index.md` if new sections are added. Append a `## [YYYY-MM-DD] ingest | issue-013 notes step pitch selection` entry to `Notes/wiki/log.md`. Do not modify the parent issue or the parent PRD.

---

### 12. Code walkthrough

**Type**: CODE WALKTHROUGH
**Output**: `Notes/walkthroughs/issue-013-notes-step-pitch-selection/code-walkthrough/` containing the files generated by `uvx showboat` for this implementation.
**Depends on**: 11

Run `uvx showboat --help` for current usage, then generate a walkthrough of the issue-013 implementation into a new directory `Notes/walkthroughs/issue-013-notes-step-pitch-selection/code-walkthrough/`. The walkthrough should cover: (1) the `validatePitchSelection` pure function (cardinality rules, the exact two-hand message, unavailable-pitch rejection, hostile-shape tolerance), (2) the `resolvePitchSelectionState` pure function (first-derivation all-selected default, non-re-expansion, re-derivation after Issue 11 clear), (3) the `updateEtudePitches` repository function (CAS persistence, version increment, identical-resubmit short-circuit, stale-version and epoch-mismatch rejections, `notesConfirmed` stays false), (4) the `resolveCanonicalRoute` extension to `/etude/notes`, (5) the `GET /etude/notes` form rendering (available-pitch derivation, first-derivation vs stored selection, error-summary wiring), and (6) the `POST /etude/notes` handler (ordinary save with cardinality validation, Select all without scripting, stale-version rejection, redisplay of narrowed selection with cardinality error). Place all generated files there.

---

### 13. Human review against the PRD and cross-cutting contract

**Type**: REVIEW
**Output**: A human verifies the implementation against the issue's "How to verify" and "Acceptance criteria" sections, the PRD's user stories 13, 14, and 15, cross-cutting contract §2 (the whole parameter-form contract: hidden `workflowVersion` with compare-and-set, safe invalid-value redisplay through the one-time validation state, focused accessible error summary and field-error wiring, native constraints, hostile form shapes, and downstream invalidation), §4 (token table row for `POST /etude/notes`: `workflowVersion` CAS, increments), and §5 (the notes step is one coherent prerequisite — confirmed only when both pitches and durations are confirmed; this issue confirms the pitch half only), confirming every acceptance criterion in the parent issue is met.
**Depends on**: 12

This is a human-in-the-loop step. The human must verify: (a) given a newly derived notes step, every available pitch is selected; (b) given both hands selected, when fewer than two pitches are submitted, the submission is rejected with the exact field-level message "Select at least two pitches when using both hands."; (c) given one hand selected, zero pitches is rejected and one pitch is accepted; (d) a submitted pitch not in the derived available set is rejected and nothing is persisted; (e) Select all activated with scripting unavailable round-trips through the server, all available pitches become selected and persisted, and no cardinality error results; (f) after an Issue 11 clear of the pitch selection, the next render is a first derivation with the full new available set preselected (not the previous selection carried over or intersected); (g) a stored narrowed selection is shown as-is on re-render and is not re-expanded; (h) a rejected pitch submission redisplays the student's narrowed selection with the group-level error, the error summary is focused, and nothing is persisted; (i) a stale workflow version is rejected and the currently saved selection is shown with an explanatory error; (j) duplicate, reordered, empty, or unknown pitch field values each resolve to a deterministic accept or field-addressable reject and never a 500; (k) a successful pitch save confirms the pitch half but the notes step itself is complete only once durations are confirmed too (`notesConfirmed` stays false). Record the result in the review notes. Do not modify the parent issue or the parent PRD.

---
